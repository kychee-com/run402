# Documentation Map

Single source of truth for every agent- and developer-facing documentation surface in the run402 ecosystem. **Use this as a checklist when changing code:** for each surface you touched, scan the tables below and update every doc that mentions it.

The same code change rarely lands in just one doc. A new MCP tool needs a row in [README.md](README.md) *and* a tools-by-category entry in [SKILL.md](SKILL.md) *and* a section in [llms-mcp.txt](llms-mcp.txt) *and* a `SURFACE` entry in `sync.test.ts`. A new CLI subcommand belongs in [cli/llms-cli.txt](cli/llms-cli.txt) *and* [openclaw/SKILL.md](openclaw/SKILL.md). A new SDK method belongs in [sdk/llms-sdk.txt](sdk/llms-sdk.txt). Don't ship without scanning.

## How to use

1. Identify what you changed.
2. Scan the **Status** column to know which docs are already OK vs need work.
3. Read the **Update when…** column for the surface you touched.
4. Update every flagged doc. For private-repo docs, open a separate PR there.

### Status legend

- 🟢 **Up-to-date** with the current modern surface; no known gaps
- 🟡 **Stale or partial** — present and not actively wrong, but missing recent features OR an auto-fix-pending deploy-pulled copy
- 🔴 **Broken** — actively wrong info, missing entirely, or misleading scope
- ⚪ **Not audited / out of scope for this pass**

Status is a snapshot — refresh it whenever the underlying doc changes.

## Public repo (this repo)

| Status | File | Audience | What's there | Update when… |
|:------:|------|----------|--------------|--------------|
| 🟢 | [`README.md`](README.md) | Humans on GitHub + the [`run402-mcp` npm page](https://www.npmjs.com/package/run402-mcp) | Five-interface overview, the modern patterns (paste-and-go AssetRef, expose manifest, deploy-dir, `site.public_paths`, static route targets + URL diagnostics, deploy release inventory/diff reads, GitHub Actions OIDC, in-function helpers), MCP install matrix per host | A tool is added/removed; the patterns evolve; a package joins/leaves lockstep |
| 🟢 | [`README.zh-CN.md`](README.zh-CN.md) | Chinese-reading humans on GitHub + the [`run402-mcp` npm page](https://www.npmjs.com/package/run402-mcp) | Simplified-Chinese mirror of `README.md` (the run402-mcp pitch + quickstart). Linked from `README.md` (`[简体中文](./README.zh-CN.md)`) and links back via `[English](./README.md)` | `README.md` is restructured or its content meaningfully changes — re-translate to keep the mirror in sync (it can silently drift) |
| 🟢 | [`AGENTS.md`](AGENTS.md) | AI coding agents working *inside* this repo (Claude Code, Codex, Cursor, Cline) | Architecture, build/test commands, SDK kernel layout (25 namespaces, incl. `r.orgs` / `r.grants` / `r.wallets`), unified deploy v1.34+ details including release observability, error patterns, npm workspaces note. Also links here. | Architecture or build/test steps change; a new SDK namespace is added; a new error helper lands; deploy primitive changes |
| 🟢 | [`CLAUDE.md`](CLAUDE.md) | Claude Code | One line: `@AGENTS.md` (just an import) | Never |
| 🟢 | [`SKILL.md`](SKILL.md) | MCP-host agents (Claude Desktop, Cursor, Cline, Claude Code) — they have `run402-mcp` tools loaded | Teaches the platform via **MCP tool names** in natural-language framings. Quickstart, the four patterns, `site.public_paths`, static route targets + `deploy_diagnose_url`, release observability with `static_public_paths`, tools by category, troubleshooting matrix. Frontmatter `install: run402-mcp`. | A tool is added/removed; a pattern changes; a troubleshooting case is discovered |
| 🟢 | [`openclaw/SKILL.md`](openclaw/SKILL.md) | OpenClaw script-runtime agents | Same content as `SKILL.md` but every verb is `run402 <…>` (CLI shape), including `run402 deploy release <get|active|diff>`, `run402 deploy diagnose`, `run402 deploy resolve`, `site.public_paths`, and `static_public_paths` observability. Frontmatter `install: run402` (the CLI). | A CLI subcommand is added/removed; a pattern changes; a troubleshooting case is discovered |
| 🟢 | [`SKILL.test.ts`](SKILL.test.ts) | CI | Validates frontmatter + required CLI verbs / MCP tool names / banned regressions for **both** SKILL files | Whenever a "canonical" verb or tool name moves in/out of either skill |
| 🟢 | [`cli/README.md`](cli/README.md) | Humans on GitHub + the [`run402` npm page](https://www.npmjs.com/package/run402) | 30-second start, common-command examples per category, GitHub Actions OIDC quickstart/workflow shape, state-on-disk note, **points at `llms-cli.txt` as authoritative** | Major CLI surface change; install flow change; new top-level command group |
| 🟢 | [`llms.txt`](llms.txt) | AI agents fetching <https://run402.com/llms.txt> (entry point) | High-level wayfinder. Vision, what we offer, the four integration surfaces (SDK ⭐ recommended, CLI, MCP, raw HTTP), GitHub Actions OIDC pointer, pricing summary, 30-second start, links to each surface's deep reference. Served at run402.com/llms.txt via private-repo deploy pulling this file | Vision / what-we-offer / pricing changes; new integration pattern added; surface-recommendation order changes |
| 🟢 | [`cli/llms-cli.txt`](cli/llms-cli.txt) | AI agents fetching <https://docs.run402.com/llms-cli.txt> | Comprehensive CLI reference. Every subcommand, every flag, deploy release inventory/diff commands, `site.public_paths`, `run402 deploy diagnose`/`resolve`, GitHub Actions OIDC flow, `run402 operator` (human/email session) commands, generated workflow shape, troubleshooting. Served at docs.run402.com via this repo's OIDC deploy (`deploy-docs.yml`) | **Any** CLI subcommand or flag added/removed/renamed |
| 🟢 | [`sdk/llms-sdk.txt`](sdk/llms-sdk.txt) | AI agents fetching <https://docs.run402.com/llms-sdk.txt> | Comprehensive SDK reference. Two entry points, every SDK namespace with method signatures (incl. `r.operator`, the human/email session, and the `r.orgs` / `r.grants` org-and-delegation pillar), CI/OIDC helpers and credential semantics, the modern patterns, full `Run402Error` hierarchy with `Run402DeployError` envelope, `ReleaseSpec` + `site.public_paths` + static route targets + release inventory/diff/resolve shapes, stability + lockstep notes. Served at docs.run402.com via this repo's OIDC deploy (`deploy-docs.yml`) | Any SDK method or namespace added/changed; `ReleaseSpec` shape changes; new error class lands |
| 🟢 | [`llms-mcp.txt`](llms-mcp.txt) | AI agents fetching <https://docs.run402.com/llms-mcp.txt> | Comprehensive MCP reference. Every tool with parameters, deploy release inventory/diff/diagnose tools, `site.public_paths`, the four patterns, error-envelope branching, troubleshooting, install snippets per host. Served at docs.run402.com via this repo's OIDC deploy (`deploy-docs.yml`) | Any MCP tool added/removed/renamed; pattern changes; new install host needs documenting |
| 🟢 | [`sdk/README.md`](sdk/README.md) | TypeScript devs / agents using the SDK; the [`@run402/sdk` npm page](https://www.npmjs.com/package/@run402/sdk) | Two entry-point install matrix (Node vs isomorphic), full SDK namespace catalog table (incl. `wallets`, `orgs`, `grants`), paste-and-go AssetRef pattern, unified `r.project(id).apply` primitive (with three layers + `fileSetFromDir`) plus release inventory/diff reads, CI/OIDC helpers, full error hierarchy including `Run402DeployError`, stability + lockstep notes | New SDK namespace / method; new error class; entry-point changes |
| 🟢 | [`astro/README.md`](astro/README.md) | Astro devs / agents using `@run402/astro`; the [`@run402/astro` npm page](https://www.npmjs.com/package/@run402/astro) | **v2.x stable SSR runtime** (currently 2.4.1) + image-variant pipeline + hosted human auth. One-line `export default run402();` returns an AstroUserConfig composing the SSR adapter (Lambda + SnapStart + ISR cache + ALS request context) + image integration + build-time hard-fail detectors (dynamic `<Image>` / server islands / sessions). **Authentication section**: four hosted-auth components `<SignIn>` / `<SignUp>` / `<UserButton>` / `<AccountSecurity>` plus `<SignedIn>` / `<SignedOut>` render gates (CSRF, freshness step-up, re-auth, session rotation, passkey/OAuth ceremonies — the vision-pillar-2 hosted-auth surface; full `R402_AUTH_*` reference at run402.com/errors). Runtime CMS image components `<Run402Picture asset>` (v1.0) and `<Run402Image>` (v1.51+ — pre-decoded blurhash placeholder, strict-mode degradation detection, Astro + `/react` entry points). Structured `R402_*` error-code reference across the auth / image / SSR-runtime / bundle / cache / deploy families (~30 codes; the SSR-runtime subsection alone lists 17 — verify the exact count against `astro/README.md` at edit time). v0.2.x named `run402` aliased to `run402Image` for backwards-compat. Independent release cadence via `/publish-astro` (OIDC Trusted Publisher). | New `<Image>` / `<Run402Picture>` / `<Run402Image>` prop or option; new hosted-auth component or auth component prop; preset option added; SSR adapter hook changes; data-driven pattern guidance changes; new R402_* code; image-pipeline features need wiring |
| 🟢 | [`CHANGELOG.md`](CHANGELOG.md) | Anyone reading the npm changelog or upgrading `run402-mcp` / `run402` / `@run402/sdk` | Per-version notes for the three in-repo lockstep packages (`@run402/sdk`, `run402` CLI, `run402-mcp`). `## Unreleased` plus dated/versioned sections, newest first. `@run402/functions` and `@run402/astro` keep their own changelogs. | Every lockstep version bump (run402-mcp / run402 / @run402/sdk) — add notes under `## Unreleased` as code lands, cut the version section at publish |
| 🟢 | [`astro/CHANGELOG.md`](astro/CHANGELOG.md) | Anyone reading the npm changelog or upgrading | Per-version notes for `@run402/astro`. Current 2.x line (2.0.0 = hosted-auth chrome components `<SignIn>` / `<SignUp>` / `<UserButton>` / `<SignedIn>` / `<SignedOut>`, paired with `@run402/functions` v3.0 + gateway v1.60/v1.61 `auth-aware-ssr`, plus the BREAKING `@run402/functions` peer bump to `^3.0.0`), with the 1.0.0-alpha.1 SSR-preset, 0.3.0-alpha.1 additive-SSR-exports, and 0.2.4 image-integration history below it, and migration notes between versions. (Published at 2.4.1; the `## Unreleased` block carries the Astro-6 adapter migration + `astro` peer-range tightening.) | Every `@run402/astro` version bump |
| 🟢 | `@run402/functions` package docs | TypeScript devs / agents using `@run402/functions`; the [`@run402/functions` npm page](https://www.npmjs.com/package/@run402/functions) | In-function helper API — `db(req)` / `adminDb()` / `auth.*` (`auth.user`, `auth.requireUser`, `auth.requireRole`, `auth.requireMembership`, `auth.fetch`, `auth.sessions.*`, `auth.identities.link`; v3.0 retired the bare `getUser`/`getUserId`/`getRole` exports — they throw `R402_AUTH_UNKNOWN_EXPORT`) / `email` / `ai` / `assets` / routed-http with examples, fluent surface, `adminDb().sql()` parameterized SQL, SSG build-time use, deploy-time bundling note ("don't list in `--deps`"), v1.51+ "Function-level auth gates" section covering `FunctionSpec.requireAuth` / `requireRole` shape, header injection, deploy-time validation, and cache-staleness footnote. **Source lives in `kychee-com/run402-private` at `packages/functions/`** so it can co-evolve with the gateway that bundles it; the npm package on the registry is unchanged. | `db`/`adminDb`/`auth.*`/`email`/`ai`/`assets` surface changes; new in-function helper lands; bundling rules change — edit `packages/functions/README.md` in the private repo |
| 🟢 | [`openclaw/README.md`](openclaw/README.md) | Humans browsing the `openclaw/` folder on GitHub | Install instructions, the script-runtime mechanism (re-export from CLI), pointer at `openclaw/SKILL.md` for the full body, the SKILL.md (root) vs openclaw/SKILL.md split, modern patterns callout (paste-and-go / expose manifest / deploy-dir / `site.public_paths` / db(req)), OpenClaw-vs-MCP comparison | Major skill or install-flow change; SKILL split changes |
| 🟢 | [`documentation.md`](documentation.md) (this file) | Anyone updating docs | The map | A new doc surface is added; a doc moves / is renamed; status changes |

**This repo is public.** Don't add internal documentation, design specs, consultation transcripts, or runbooks here — they belong in `~/Developer/run402-private/docs/`. If you run `/consult` or generate any internal-thinking artifact, move the resulting file to the private repo before committing.

## Private repo (`run402-private/apps/marketing/`)

Only listed here are files that **originate in the private repo** — i.e., authored and edited there directly. Deploy-pulled mirrors of public-repo files (the public-repo source rows above are authoritative — edit those, then redeploy the site to refresh) are deliberately omitted. Marketing / legal / topic pages are website concerns, also out of scope.

| Status | File | Audience | What's there | Update when… |
|:------:|------|----------|--------------|--------------|
| 🟢 | `apps/marketing/llms-full.txt` | AI agents fetching <https://run402.com/llms-full.txt> — the HTTP API reference | Comprehensive HTTP / `curl`-shaped reference: every endpoint, request/response shapes, auth header rules, pricing, lifecycle. ~2,180 lines. (Verified live 2026-06-18.) | API surface (HTTP routes, request/response shape, auth) changes; new endpoint ships |
| 🟢 | `apps/marketing/openapi.json` | Machines and docs tooling consuming <https://run402.com/openapi.json> | OpenAPI schema for the HTTP API, including `/apply/v1/releases/{release_id}`, `/apply/v1/releases/active`, `/apply/v1/releases/diff`, `ReleaseInventory`, and `ReleaseToReleaseDiff`. | API routes, query params, response schemas, auth, or error envelopes change |
| 🟢 | `apps/marketing/updates.txt` | AI-readable changelog (`/updates.txt`) | `## YYYY-MM-DD` headings, newest first. Covers expose manifests, CAS substrate, agent-DX blob CDN, unified apply, assets-as-first-class with `syncDir`/`uploadDir`/`prepareDir`, deploy-time functions bundling, error envelopes, and `/apply/v1` hardening backfills | New entry on every user-visible feature ship |
| 🟢 | `apps/marketing/humans/changelog.html` | Human-readable changelog (served at `/humans/changelog.html`) | Mirrors `updates.txt` in prose form, authored/edited directly as HTML — there is no markdown source. `<h2>` headings newest-first in "Month DD, YYYY" form (verified coverage 2026-03-23 → 2026-06-17) | New entry in lockstep with `updates.txt` |

## Common change → docs to update

When you… | Update at minimum…
---|---
**Add or remove an MCP tool** | `src/index.ts` (registration), `sync.test.ts` (`SURFACE` + `SDK_BY_CAPABILITY`), [README.md](README.md) (tools table), [SKILL.md](SKILL.md) (tools by category), [llms-mcp.txt](llms-mcp.txt) (tools by category), `cli/llms-cli.txt` if there's a sibling CLI subcommand
**Add or remove a CLI subcommand** | `cli/lib/<group>.mjs` (impl), `sync.test.ts` (`SURFACE`), [cli/llms-cli.txt](cli/llms-cli.txt), [openclaw/SKILL.md](openclaw/SKILL.md) (CLI verbs), [cli/README.md](cli/README.md) only if it's a new top-level group, mirror sibling MCP tool docs
**Add an SDK namespace or method** | `sdk/src/namespaces/<name>.ts`, `sdk/src/index.ts` (constructor wiring — the canonical namespace set is the `this.<name> = new …` list here; 25 today), [sdk/llms-sdk.txt](sdk/llms-sdk.txt) (namespace section + signatures), [sdk/README.md](sdk/README.md) (namespace catalog table — add the row; prefer count-agnostic phrasing over a hardcoded total), [AGENTS.md](AGENTS.md) (architecture diagram + kernel namespace count/enumeration), [README.md](README.md) (namespace enumeration — add the new namespace to the list, not just the number), `sync.test.ts` (`SDK_BY_CAPABILITY`)
**Add deploy observability surface** (release inventory / active / release-to-release diff) | SDK deploy types + methods, scoped wrappers, CLI `run402 deploy release <get|active|diff>`, MCP `deploy_release_*` tools, `sync.test.ts`, [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [sdk/README.md](sdk/README.md), [cli/llms-cli.txt](cli/llms-cli.txt), [cli/README.md](cli/README.md), [llms-mcp.txt](llms-mcp.txt), [SKILL.md](SKILL.md), [openclaw/SKILL.md](openclaw/SKILL.md), [README.md](README.md), [AGENTS.md](AGENTS.md), plus private `apps/marketing/openapi.json`, `apps/marketing/llms-full.txt`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Add or change stable static asset identity / public URL diagnostics** | SDK deploy types + `Deploy.resolve` helpers + scoped wrapper, `site.public_paths`, `static_public_paths` / `reachability_authority` observability, stable-host fields such as `authorization_result`, `cas_object`, `response_variant`, `allow`, `route_pattern`, `target_type`, `target_name`, and `target_file`, static route target validation, Node manifest normalization, CLI `run402 deploy diagnose` / `run402 deploy resolve`, MCP `deploy_diagnose_url`, OpenClaw parity, `sync.test.ts` (`SURFACE`, `SDK_BY_CAPABILITY`, docs drift guards for literals such as `active_release_missing`, `unsupported_manifest_version`, `negative_cache_hit`, `route_function`, `route_static_alias`, and `route_method_miss`), [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [sdk/README.md](sdk/README.md), [cli/llms-cli.txt](cli/llms-cli.txt), [cli/README.md](cli/README.md), [llms-mcp.txt](llms-mcp.txt), [SKILL.md](SKILL.md), [openclaw/SKILL.md](openclaw/SKILL.md), [openclaw/README.md](openclaw/README.md), [README.md](README.md), [AGENTS.md](AGENTS.md), plus private `apps/marketing/openapi.json`, `apps/marketing/llms-full.txt`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Add or change CI/OIDC federation** | `sdk/src/namespaces/ci.ts`, `sdk/src/ci-credentials.ts`, `sdk/src/node/ci.ts`, `cli/lib/ci.mjs`, [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [sdk/README.md](sdk/README.md), [cli/llms-cli.txt](cli/llms-cli.txt), [cli/README.md](cli/README.md), [README.md](README.md), [llms.txt](llms.txt) if it remains a wayfinder-level pattern, [AGENTS.md](AGENTS.md), [openclaw/SKILL.md](openclaw/SKILL.md), plus private `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Add or change project transfer surface** (unified noun, v1.93+ — `initiate` wallet XOR email / `preview` / `accept` (wallet) / `claim` (email) / `cancel` / `list`, all kind-agnostic; `recipient_kind` on rows; `TransferFreezeError`, `tier_status.incoming_transfers[]`, `projects[].secrets_rotation_advised`) | `sdk/src/namespaces/transfers.ts` (the `Transfers` class exposed at `r.admin.transfers` — `initiate` overloads, `claim`, unified types; the five `*Handoff` methods are removed), `sdk/src/namespaces/admin.ts` (sub-namespace wiring), `sdk/src/namespaces/tier.ts` (TierStatusResult extensions), `sdk/src/errors.ts` (`TransferFreezeError` + `isTransferFreezeError`), `sdk/src/kernel.ts` (409 PROJECT_HAS_PENDING_TRANSFER detection), `sdk/src/index.ts` (re-exports), `src/tools/transfers.ts` (seven MCP tools incl. `claim_project_transfer` + email `to_email` on initiate), `src/index.ts` (registration), `src/errors.ts` (PROJECT_HAS_PENDING_TRANSFER guidance), `cli/lib/transfer.mjs` (CLI — `--to` auto-detect, no `--handoff`/`--handoffs`), `cli/cli.mjs` (dispatch), `openclaw/scripts/transfer.mjs` (re-export), `sync.test.ts` (`SURFACE` capabilities + `SDK_BY_CAPABILITY` entries + module list in `parseCliCommands`/`parseOpenClawCommands`), [SKILL.md](SKILL.md) (Project transfer section), [openclaw/SKILL.md](openclaw/SKILL.md) (Project transfer section), plus [cli/llms-cli.txt](cli/llms-cli.txt), [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [llms-mcp.txt](llms-mcp.txt) for the comprehensive surface references, and private `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html` for the HTTP API + changelog entries.
**Add or change the operator (human/email) session surface** (`r.operator` device-auth + `overview`; `run402 operator login/logout/overview/whoami`; `core/operator-session.ts` cache) | `core/src/operator-session.ts` (cache primitive + test), `sdk/src/namespaces/operator.ts` + `sdk/src/index.ts` (namespace wiring + `export type *`), `cli/lib/operator.mjs` + `cli/cli.mjs` (dispatch + top-level HELP), `openclaw/scripts/operator.mjs` (re-export), `sync.test.ts` (`SURFACE` rows with **`mcp: null`** + `SDK_BY_CAPABILITY` + the `parseCliCommands`/`parseOpenClawCommands` module list + `operator.devicePoll` in `SDK_ONLY_METHODS`), `cli-help.test.mjs` MATRIX, [cli/llms-cli.txt](cli/llms-cli.txt), [sdk/llms-sdk.txt](sdk/llms-sdk.txt) (namespace section + count), [sdk/README.md](sdk/README.md) (catalog row + count), [README.md](README.md) + [AGENTS.md](AGENTS.md) (namespace count; AGENTS Shared Core entry), [cli/README.md](cli/README.md), [openclaw/SKILL.md](openclaw/SKILL.md). **No MCP tool by design — skip [SKILL.md](SKILL.md) / [llms-mcp.txt](llms-mcp.txt).** Gateway device-auth bridge (RFC 8628 endpoints) lives in `run402-private`.
**Add or change the control-plane SESSION surface** (v1.78 `passkey-principals-onboarding` — the write-capable human session: `r.operator.session.*` hosted login/step-up/authenticators/recovery, `controlPlaneSessionCredentials`, `StepUpRequiredError`, loopback-PKCE `operator.buildCliAuthorizeUrl`/`exchangeCliToken`, `core/control-plane-session.ts` cache, invite→claim-at-login) | `sdk/src/namespaces/operator-session.ts` (the `OperatorSession` class at `r.operator.session`) + `sdk/src/namespaces/operator.ts` (sub-object wiring) + `sdk/src/control-plane-credentials.ts` + `sdk/src/index.ts` (re-exports), `core/src/control-plane-session.ts` (cache), `cli/lib/operator.mjs` (loopback login + memberships surfacing), `sync.test.ts` (`operator.session.*` + loopback + credential helpers in `SDK_ONLY_METHODS` — browser-interactive, **no MCP/CLI verb**), unit tests `sdk/src/namespaces/operator-session.test.ts` + `sdk/src/control-plane-credentials.test.ts`, [sdk/llms-sdk.txt](sdk/llms-sdk.txt) (`r.operator` write-session subsection), [sdk/README.md](sdk/README.md) (operator catalog row), [cli/llms-cli.txt](cli/llms-cli.txt) (org invite claim + `operator login --loopback`), [AGENTS.md](AGENTS.md) (Shared Core entry). **No MCP tool / no new CLI verb by design** — the hosted login is browser-interactive (console-side); skip [SKILL.md](SKILL.md) / [llms-mcp.txt](llms-mcp.txt). Gateway routes + the OAuth bridges live in `run402-private`.
**Add or change org / membership / grants / delegation surface** (`r.orgs` / `r.org(id)` / `r.grants`; org create/get/rename/list/whoami/audit, member add/role/rm/list, invite create/list/rm, project grant create/revoke — vision pillar 2: org-owned ownership + bounded revocable agent delegation) | `sdk/src/namespaces/org.ts` + `org.types.ts`, `sdk/src/namespaces/grants.ts` + `grants.types.ts`, `sdk/src/index.ts` (`Orgs`/`ScopedOrg`/`Grants` imports + `r.orgs`/`r.grants` wiring + the `r.org(id)` scoped sub-client + re-exports + namespace count), `src/tools/orgs.ts` + `src/tools/grants.ts` + `src/index.ts` (MCP registration), `cli/lib/org.mjs` + `cli/lib/grants.mjs` + `cli/cli.mjs` (`org`/`grants` dispatch, incl. nested `org member` / `org invite`), `openclaw/scripts/org.mjs` + `openclaw/scripts/grants.mjs` (re-exports), `sync.test.ts` (`SURFACE` org/grant rows + `SDK_BY_CAPABILITY` + `org`/`grants` in `parseCliCommands`/`parseOpenClawCommands` + `parseOrgGroupActions` member/invite leaves), [sdk/llms-sdk.txt](sdk/llms-sdk.txt) ("Org membership & project grants"), [sdk/README.md](sdk/README.md) (catalog rows + count), [llms-mcp.txt](llms-mcp.txt), [SKILL.md](SKILL.md), [openclaw/SKILL.md](openclaw/SKILL.md), [cli/llms-cli.txt](cli/llms-cli.txt), [README.md](README.md) + [AGENTS.md](AGENTS.md) (namespace count). Gateway org-owned control plane (`/orgs/v1/*` + project grants) lives in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Add or change the forkable-apps / marketplace surface** (`r.apps` `browse`/`getApp`/`fork`/`publish`/`listVersions`/`updateVersion`/`deleteVersion`; MCP `browse_apps`/`get_app`/`fork_app`/`publish_app`/`list_versions`/`update_version`/`delete_version`; CLI `run402 apps browse|inspect|fork|publish|versions|update|delete` — vision pillars 3-4: the "one paid call, one independent fully-deployed copy" fork) | `sdk/src/namespaces/apps.ts`, `sdk/src/index.ts` (`r.apps` wiring + re-exports), `src/tools/{browse-apps,get-app,fork-app,publish-app,list-versions,update-version,delete-version}.ts` + `src/index.ts` (registration), `cli/lib/apps.mjs` + `cli/cli.mjs` (dispatch), `openclaw/scripts/apps.mjs` (re-export), `sync.test.ts` (`SURFACE` rows + `SDK_BY_CAPABILITY` entries), [sdk/llms-sdk.txt](sdk/llms-sdk.txt) (`r.apps` section), [sdk/README.md](sdk/README.md) (catalog row + count), [llms-mcp.txt](llms-mcp.txt) (Apps tools), [SKILL.md](SKILL.md) (Apps marketplace section), [openclaw/SKILL.md](openclaw/SKILL.md), [cli/llms-cli.txt](cli/llms-cli.txt), [README.md](README.md) (Apps marketplace table) + [AGENTS.md](AGENTS.md) (namespace count). Gateway `/apps/v1` + `/fork/v1` + `/projects/v1/admin/:id/versions*` routes live in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Change `@run402/functions` surface** | edit `packages/functions/src/` AND `packages/functions/README.md` in the **private** repo (`kychee-com/run402-private`), then in this public repo update [AGENTS.md](AGENTS.md) (Functions library section), [SKILL.md](SKILL.md), [llms-mcp.txt](llms-mcp.txt), [sdk/llms-sdk.txt](sdk/llms-sdk.txt), and [openclaw/SKILL.md](openclaw/SKILL.md) (in-function helpers section in each). Publish via `/publish-functions` in the private repo.
**Change `@run402/astro` surface** (new `<Image>` / `<Run402Picture>` / `<Run402Image>` prop or `imageDefaults` option, a hosted-auth component prop on `<SignIn>` / `<SignUp>` / `<UserButton>` / `<AccountSecurity>` / `<SignedIn>` / `<SignedOut>`, an integration option, a new `R402_ASTRO_*` / `R402_AUTH_*` error code, data-driven pattern guidance, or a runtime helper) | edit `astro/src/` and [astro/README.md](astro/README.md) and [astro/CHANGELOG.md](astro/CHANGELOG.md). If the change touches the AssetRef-persistence vs manifest pattern guidance, also reflect it in [AGENTS.md](AGENTS.md) (Astro integration section). Publish via `/publish-astro` (OIDC Trusted Publisher → `.github/workflows/publish-astro.yml`) — independent of the SDK/CLI/MCP lockstep.
**Add SSR Runtime / cache layer surface** (run402 `cache` / `dev` / `doctor` / `init astro` / `logs --request-id`; `r.cache` SDK; SSR adapter hooks; R402_* error codes; cache hot-path debug headers) | edit `cli/lib/<group>.mjs` (impl), `cli/cli.mjs` (dispatch), `openclaw/scripts/<group>.mjs` (CLI parity re-export), [cli/llms-cli.txt](cli/llms-cli.txt) (R402_* codes table + new sections), `sdk/src/namespaces/cache.ts` + `sdk/src/index.ts` (type re-export) + [sdk/llms-sdk.txt](sdk/llms-sdk.txt) (`r.cache` namespace), `astro/src/` + [astro/README.md](astro/README.md) + [astro/CHANGELOG.md](astro/CHANGELOG.md) (preset + adapter + detectors + `<Run402Picture>` + `<Run402Image>` + hosted-auth components `<SignIn>` / `<SignUp>` / `<UserButton>` / `<AccountSecurity>` + `<SignedIn>` / `<SignedOut>` gates), [AGENTS.md](AGENTS.md) (namespace count + v1.52 architecture section), [README.md](README.md) (namespace count + SSR pitch), [SKILL.md](SKILL.md) + [openclaw/SKILL.md](openclaw/SKILL.md) (Astro SSR runtime + ISR cache pattern section), [llms.txt](llms.txt) (vision bullet), [sdk/README.md](sdk/README.md) (namespace catalog table — bump count + cache row), `sync.test.ts` (`SURFACE` capabilities + `SDK_BY_CAPABILITY` entries + module list in `parseCliCommands`/`parseOpenClawCommands` + `SDK_ONLY_METHODS` for flag-variant convenience methods). Cross-repo to `run402-private`: edit `packages/gateway/src/` (substrate + hot path + apply-v1 plumbing), `packages/functions/src/` (SDK ALS + `cache.*`), `openspec/changes/astro-ssr-runtime/`, `apps/marketing/llms-full.txt` (new `/cache/v1/*` endpoints), `apps/marketing/openapi.json` (cache endpoint schemas), `apps/marketing/updates.txt` + `apps/marketing/humans/changelog.html` (user-visible feature entry).
**Rename / remove a tool, command, flag, or method** | All of the above + verify [SKILL.test.ts](SKILL.test.ts) banned-pattern list catches it; cross-repo: drop a private `apps/marketing/updates.txt` entry if user-visible
**Change a pattern** (auth-as-SDLC, paste-and-go AssetRef, deploy-dir plan/commit, db(req) vs adminDb()) | [README.md](README.md) patterns section, both `SKILL.md` files, [cli/llms-cli.txt](cli/llms-cli.txt), [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [llms-mcp.txt](llms-mcp.txt), [llms.txt](llms.txt) wayfinder if the pattern is part of the pitch
**Add a new integration surface** | [llms.txt](llms.txt) (the wayfinder is the source of truth for "what surfaces exist"), [README.md](README.md) integrations table, [AGENTS.md](AGENTS.md), a matching `llms-<surface>.txt` reference file
**Change pricing or limits** | [README.md](README.md), [SKILL.md](SKILL.md), [cli/llms-cli.txt](cli/llms-cli.txt), [sdk/llms-sdk.txt](sdk/llms-sdk.txt), [llms-mcp.txt](llms-mcp.txt), [llms.txt](llms.txt), [openclaw/SKILL.md](openclaw/SKILL.md). Cross-repo: private `apps/marketing/humans/*.html` (terms, pricing pages)
**Change architecture (kernel, error hierarchy, build flow)** | [AGENTS.md](AGENTS.md) is the canonical place; reflect any user-visible consequences in the relevant skill / reference
**Cross-repo (private repo)** — **change an HTTP endpoint, ship a user-visible feature, or change the API surface** | `apps/marketing/llms-full.txt` (HTTP API reference), `apps/marketing/openapi.json` if applicable, `apps/marketing/updates.txt` + `apps/marketing/humans/changelog.html` for user-visible features. Open a separate PR in `run402-private/`.

## Notes on canonicality

- **`llms.txt`** (root) is canonical for the wayfinder — the entry-point document at `run402.com/llms.txt`. Deploy-pulled.
- **`cli/llms-cli.txt`** is canonical for the comprehensive CLI reference. Served at docs.run402.com via this repo's OIDC deploy.
- **`sdk/llms-sdk.txt`** is canonical for the comprehensive SDK reference. Served at docs.run402.com via this repo's OIDC deploy.
- **`llms-mcp.txt`** (root) is canonical for the comprehensive MCP reference. Served at docs.run402.com via this repo's OIDC deploy.
- **`SKILL.md`** (root) is canonical for the MCP-host skill body; served at docs.run402.com via this repo's OIDC deploy (the apex agent-skills index references it).
- **`openclaw/SKILL.md`** is canonical for the OpenClaw script-runtime skill body — bundled into the OpenClaw skill install.
- **`AGENTS.md`** is canonical for repo architecture; `CLAUDE.md` just imports it.
- **`apps/marketing/llms-full.txt`** (private repo) is canonical for the HTTP API reference — edit directly there.
- **`apps/marketing/updates.txt`** + **`apps/marketing/humans/changelog.html`** (private repo) are canonical for the changelog — edit directly there (the changelog is authored as HTML; there is no markdown source).
