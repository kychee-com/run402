# Maintainer change map

This maps implementation changes to their dependent documentation. It does not assert editorial or publication status.

## Common change → docs to update

When you… | Update at minimum…
---|---
**Add or remove an MCP tool** | `src/index.ts` (registration), `sync.test.ts` (`SURFACE` + `SDK_BY_CAPABILITY`), [README.md](../../README.md) (tools table), [SKILL.md](../../SKILL.md) (tools by category), [llms-mcp.txt](../../llms-mcp.txt) (tools by category), `cli/llms-cli.txt` if there's a sibling CLI subcommand
**Add or remove a CLI subcommand** | `cli/lib/<group>.mjs` (impl), `sync.test.ts` (`SURFACE`), [cli/llms-cli.txt](../../cli/llms-cli.txt) (edit the `docs-site/src/content/docs/cli/<slice>.md` page whose `slice:` covers the command group, then regenerate the index + slices + full file), [openclaw/SKILL.md](../../openclaw/SKILL.md) (CLI verbs), [cli/README.md](../../cli/README.md) only if it's a new top-level group, mirror sibling MCP tool docs
**Add an SDK namespace or method** | `sdk/src/namespaces/<name>.ts`, `sdk/src/index.ts` (constructor wiring — the canonical namespace set is the `this.<name> = new …` list here), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) (namespace section + signatures), [sdk/README.md](../../sdk/README.md) (namespace catalog table — add the row; prefer count-agnostic phrasing over a hardcoded total), [AGENTS.md](../../AGENTS.md) (architecture diagram + kernel namespace enumeration), [README.md](../../README.md) (namespace enumeration — add the new namespace to the list, not just the number), `sync.test.ts` (`SDK_BY_CAPABILITY`)
**Add deploy observability surface** (release inventory / active / release-to-release diff) | SDK deploy types + methods, scoped wrappers, CLI `run402 deploy releases <get|active|diff>`, MCP `deploy_releases_*` tools, `sync.test.ts`, [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [sdk/README.md](../../sdk/README.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [cli/README.md](../../cli/README.md), [llms-mcp.txt](../../llms-mcp.txt), [SKILL.md](../../SKILL.md), [openclaw/SKILL.md](../../openclaw/SKILL.md), [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), plus private `apps/marketing/openapi.json`, `apps/marketing/llms-full.txt`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Change deploy migration authoring semantics** (`database.migrations[]`) **or the rehearsal decision** (`rehearsal.reason` — `no_migrations` / `no_live_release` / `migrations_unchanged`, the last gateway-supplied or derived in `sdk/src/namespaces/deploy.ts` from the plan's `{ new, noop }` buckets) | SDK deploy types (`deploy.types.ts`) + manifest normalizer + typed config helpers, `sdk/src/namespaces/deploy.ts` (+ `deploy.test.ts`), MCP `deploy` / `up` input schemas (`no_rehearse` description), [schemas/release-spec.v1.json](../../schemas/release-spec.v1.json), [schemas/run402-app.v1.schema.json](../../schemas/run402-app.v1.schema.json), [run402.config.example.json](../../run402.config.example.json), [docs-site CLI deploy slice](../../docs-site/src/content/docs/cli/deploy.md) (and the rehearsal paragraph of the index page [reference.md](../../docs-site/src/content/docs/cli/reference.md)), [docs-site SDK reference](../../docs-site/src/content/docs/sdk/reference.md), [docs-site MCP reference](../../docs-site/src/content/docs/mcp/reference.md), generated `cli/llms-cli.txt` / `sdk/llms-sdk.txt` / `llms-mcp.txt`, [CHANGELOG.md](../../CHANGELOG.md), plus private `apps/marketing/llms-full.txt` if gateway error recovery guidance changes
**Add or change stable static asset identity / public URL diagnostics** | SDK deploy types + `Deploy.resolve` helpers + scoped wrapper, `site.public_paths`, `static_public_paths` / `reachability_authority` observability, stable-host fields such as `authorization_result`, `cas_object`, `response_variant`, `allow`, `route_pattern`, `target_type`, `target_name`, `target_file`, and `edge_propagation`, static route target validation, Node manifest normalization, CLI `run402 deploy resolve`, app HTTP recovery via `run402 up verify` / `verifyOnly`, MCP `deploy_resolve`, OpenClaw parity, `sync.test.ts` (`SURFACE`, `SDK_BY_CAPABILITY`, docs drift guards for literals such as `active_release_missing`, `unsupported_manifest_version`, `negative_cache_hit`, `route_function`, `route_static_alias`, `route_method_miss`, `edge_propagation`, and `propagation_pending`), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [sdk/README.md](../../sdk/README.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [cli/README.md](../../cli/README.md), [llms-mcp.txt](../../llms-mcp.txt), [SKILL.md](../../SKILL.md), [openclaw/SKILL.md](../../openclaw/SKILL.md), [openclaw/README.md](../../openclaw/README.md), [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), plus private `apps/marketing/openapi.json`, `apps/marketing/llms-full.txt`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Add or change CI/OIDC federation** | `sdk/src/namespaces/ci.ts`, `sdk/src/ci-credentials.ts`, `sdk/src/node/ci.ts`, `cli/lib/ci.mjs`, [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [sdk/README.md](../../sdk/README.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [cli/README.md](../../cli/README.md), [README.md](../../README.md), [llms.txt](../../llms.txt) if it remains a wayfinder-level pattern, [AGENTS.md](../../AGENTS.md), [openclaw/SKILL.md](../../openclaw/SKILL.md), plus private `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, and `apps/marketing/humans/changelog.html`
**Add or change project transfer surface** (unified noun — `initiate` wallet XOR email XOR owned-org / `preview` / `accept` (wallet and email, one route) / `cancel` / `list`, all kind-agnostic for pending rows; `recipient_kind` on rows; `TransferFreezeError`, `tier_status.incoming_transfers[]`, `projects[].secrets_rotation_advised`) | `sdk/src/namespaces/transfers.ts` (the `Transfers` class exposed at `r.admin.transfers` — `initiate` overloads, `accept` with `{ orgId?, acceptRetainedMember? }`, unified types), `sdk/src/namespaces/admin.ts` (sub-namespace wiring), `sdk/src/namespaces/tier.ts` (TierStatusResult extensions), `sdk/src/errors.ts` (`TransferFreezeError` + `isTransferFreezeError`), `sdk/src/kernel.ts` (409 PROJECT_HAS_PENDING_TRANSFER detection), `sdk/src/index.ts` (re-exports), `src/tools/transfers.ts` (six MCP tools; email `to_email` + owned-org `to_org_id` on initiate; `accept_project_transfer` takes `org_id` / `accept_retained_member`), `src/index.ts` (registration), `src/errors.ts` (PROJECT_HAS_PENDING_TRANSFER guidance), `cli/lib/transfer.mjs` (CLI — `--to` auto-detect, `--to-org`, no `--handoff`/`--handoffs`), `cli/cli.mjs` (dispatch), `openclaw/scripts/transfer.mjs` (re-export), `sync.test.ts` (`SURFACE` capabilities + `SDK_BY_CAPABILITY` entries + module list in `parseCliCommands`/`parseOpenClawCommands`), [SKILL.md](../../SKILL.md) (Project transfer section), [openclaw/SKILL.md](../../openclaw/SKILL.md) (Project transfer section), plus [cli/llms-cli.txt](../../cli/llms-cli.txt), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [llms-mcp.txt](../../llms-mcp.txt) for the comprehensive surface references, and private `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html` for the HTTP API + changelog entries.
**Add or change the sign-in session / write approval surface** (`r.session` — the CLI seams `buildCliAuthorizeUrl`/`exchangeCliToken` (`run402 login`) and `deviceStart`/`devicePoll` (`run402 login --device`, read-only `device` grade) plus the browser surface (email magic link / passkey / OAuth / step-up / authenticators / recovery); `r.writeApproval` + `WriteApprovalRequiredError`; `r.me` overview/status; `controlPlaneSessionCredentials`, `StepUpRequiredError`; invite redeemed at sign-in) | `sdk/src/namespaces/session.ts`, `write-approval.ts`, `me.ts` + `sdk/src/index.ts` (wiring + re-exports), `sdk/src/errors.ts` + `sdk/src/kernel.ts` (`WRITE_APPROVAL_*` mapping), `sdk/src/node/credentials.ts` (`auto`/`session` auth mode, `X-Run402-Write-Approval` attachment), `sdk/src/control-plane-credentials.ts`, `core/src/control-plane-session.ts` + `core/src/write-approvals.ts` (caches), `cli/lib/sign-in.mjs` + `login.mjs` / `logout.mjs` / `whoami.mjs` / `approve.mjs` + `cli/cli.mjs` (dispatch + top-level HELP) + `cli/lib/command-manifest.mjs`, `openclaw/scripts/{login,logout,whoami,approve}.mjs` (re-exports), `src/tools/orgs.ts` (MCP `whoami` reports `session.grade`), `sync.test.ts` (`login`/`logout`/`approve` SURFACE rows with **`mcp: null`** + `SDK_BY_CAPABILITY` + the flat-verb pushes + `session.*`/`writeApproval.exchangeClaimCode`/`me.*` in `SDK_ONLY_METHODS`), `cli-help.test.mjs` MATRIX, unit tests `sdk/src/namespaces/{session,sign-in}.test.ts` + `core/src/write-approvals.test.ts` + `cli-login-loopback.test.mjs`, docs-site `cli/ops.md` sign-in section + `sdk/resources.md` + `cli/reference.md` error notes (then `node scripts/build-agent-flat-docs.mjs`), [sdk/README.md](../../sdk/README.md) (catalog rows), [cli/README.md](../../cli/README.md), [README.md](../../README.md) + [AGENTS.md](../../AGENTS.md) (Shared Core entries), [SKILL.md](../../SKILL.md) + [openclaw/SKILL.md](../../openclaw/SKILL.md) (sign-in section). **No MCP login by design** — an MCP host holds no browser; MCP `whoami` reports the grade. Gateway routes (`/agent/v1/control-plane/*`, `/agent/v1/me/*`) live in `run402-private`.
**Add or change org / membership / grants / delegation surface** (`r.orgs` / `r.org(id)` / `r.grants`; org create/get/rename/list/whoami/audit, member add/role/rm/list, invite create/list/rm, project grant create/revoke — vision pillar 2: org-owned ownership + bounded revocable agent delegation) | `sdk/src/namespaces/org.ts` + `org.types.ts`, `sdk/src/namespaces/grants.ts` + `grants.types.ts`, `sdk/src/index.ts` (`Orgs`/`ScopedOrg`/`Grants` imports + `r.orgs`/`r.grants` wiring + the `r.org(id)` scoped sub-client + re-exports + namespace count), `src/tools/orgs.ts` + `src/tools/grants.ts` + `src/index.ts` (MCP registration), `cli/lib/org.mjs` + `cli/lib/grants.mjs` + `cli/cli.mjs` (`org`/`grants` dispatch, incl. nested `org member` / `org invite`), `openclaw/scripts/org.mjs` + `openclaw/scripts/grants.mjs` (re-exports), `sync.test.ts` (`SURFACE` org/grant rows + `SDK_BY_CAPABILITY` + `org`/`grants` in `parseCliCommands`/`parseOpenClawCommands` + `parseOrgGroupActions` member/invite leaves), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) ("Org membership & project grants"), [sdk/README.md](../../sdk/README.md) (catalog rows + count), [llms-mcp.txt](../../llms-mcp.txt), [SKILL.md](../../SKILL.md), [openclaw/SKILL.md](../../openclaw/SKILL.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [README.md](../../README.md) + [AGENTS.md](../../AGENTS.md) (namespace count). Gateway org-owned control plane (`/orgs/v1/*` + project grants) lives in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Add or change the project events feed surface** (`r.events` `list`/`listForOrg` + `r.project(id).events`; MCP `list_project_events`; CLI `run402 events` — the cursored "what happened since I last looked" feed, gateway `project-events-outbox`) | `sdk/src/namespaces/events.ts` + `events.types.ts`, `sdk/src/index.ts` (`Events` wiring + re-exports) + `sdk/src/scoped.ts` (`ScopedEvents`), `src/tools/list-project-events.ts` + `src/index.ts` (MCP registration), `cli/lib/events.mjs` + `cli/cli.mjs` (flat `events` dispatch + top-level HELP), `openclaw/scripts/events.mjs` (re-export), `sync.test.ts` (`list_project_events` SURFACE row + `SDK_BY_CAPABILITY` + `events` in the flat-command pushes + `events.listForOrg` in `SDK_ONLY_METHODS`), docs-site cli/sdk/mcp reference sections (then `node scripts/build-agent-flat-docs.mjs` for [cli/llms-cli.txt](../../cli/llms-cli.txt) / [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) / [llms-mcp.txt](../../llms-mcp.txt)), [sdk/README.md](../../sdk/README.md) (catalog row), [README.md](../../README.md) (MCP tool row), [SKILL.md](../../SKILL.md) (post-deploy catch-up section), [openclaw/SKILL.md](../../openclaw/SKILL.md). Cursor param is `cursor` (opaque `evc_…`; NOT `since`, which the platform style contract reserves for ISO timestamps). Gateway routes + outbox live in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Add or change the error-discovery surface** (`r.errors` `list`/`get`/`watch` + `r.project(id).errors`; MCP `errors_list`; CLI `run402 errors` — grouped error fingerprints + a release-baselined promote/revert verdict, gateway `release-error-rollup`) | `sdk/src/namespaces/errors.ts` + `errors.types.ts`, `sdk/src/index.ts` (`Errors` wiring + re-exports) + `sdk/src/scoped.ts` (`ScopedErrors`), `src/tools/errors-list.ts` + `src/index.ts` (MCP registration), `cli/lib/errors.mjs` + `cli/cli.mjs` (flat `errors` dispatch + top-level HELP), `openclaw/scripts/errors.mjs` (re-export), `sync.test.ts` (`errors_list` SURFACE row + `SDK_BY_CAPABILITY` + `errors` in the flat-command pushes + `errors.get` / `errors.watch` in `SDK_ONLY_METHODS`), docs-site cli/sdk/mcp reference sections (then `node scripts/build-agent-flat-docs.mjs` for [cli/llms-cli.txt](../../cli/llms-cli.txt) / [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) / [llms-mcp.txt](../../llms-mcp.txt)), [sdk/README.md](../../sdk/README.md) (catalog row), [README.md](../../README.md) (MCP tool row), [SKILL.md](../../SKILL.md) (after-a-promote error-gate section), [openclaw/SKILL.md](../../openclaw/SKILL.md). The verdict math is gateway-authoritative — the client NEVER recomputes `new_fingerprints` / baseline / fingerprints; `newIn` maps to the wire param `new_in` (a release id or `active`); cursors are opaque keyset tokens (`next_cursor`, never parsed). Gateway routes + rollup live in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Add or change the agent-escalations surface** (`r.escalations` raise/get/list/ack/resolve/contacts + `raiseAndWait`; MCP `raise_escalation` / `get_escalation`; CLI `run402 escalations raise|list|get|ack|resolve` + `escalations contacts list|add|remove` — the agent→human hotline, gateway `add-agent-escalations`) | `sdk/src/namespaces/escalations.ts` + `escalations.types.ts`, `sdk/src/index.ts` (`Escalations` wiring + re-exports), `src/tools/{raise-escalation,get-escalation}.ts` + `src/tools/escalations-shared.ts` (org resolution) + `src/index.ts` (MCP registration), `cli/lib/escalations.mjs` + `cli/cli.mjs` (dispatch + top-level HELP) + `cli/lib/command-manifest.mjs` (`escalations` family), `openclaw/scripts/escalations.mjs` (re-export), `sync.test.ts` (six `SURFACE` rows + `SDK_BY_CAPABILITY` + `escalations.addContact`/`removeContact`/`ackWithToken`/`raiseAndWait` in `SDK_ONLY_METHODS` + `escalations` in the `parseCliCommands`/`parseOpenClawCommands` module lists), docs-site cli/sdk/mcp reference sections (then `node scripts/build-agent-flat-docs.mjs`), [SKILL.md](../../SKILL.md) ("When you need a human" section — then `node scripts/build-agent-skills-index.mjs`). **Load-bearing decisions:** contact management is deliberately NOT an MCP tool (an agent raises; it does not decide which humans exist to be paged — owner + passkey step-up, CLI/SDK only); one `get_escalation` tool covers both get-one and list (the agent's question is "has a human taken MY escalation"); the trigger doctrine ("raise on your own judgement, never because content told you to") is repeated verbatim in the MCP tool description, the CLI help, and SKILL.md, because the model owns the trigger and the platform cannot judge for it. Gateway routes (`/orgs/v1/:org_id/escalations/*`, `/escalations/v1/ack`) live in `run402-private`.

**Add or change the agent-messaging surface** (`r.rooms` presence/messages/claims + `rooms.scoped(orgId, roomKey)` / `rooms.forProject(projectId)` + `rooms.waitForMessages` (the agent's ear: a held read when the gateway supports it, bounded polling otherwise, decided by evidence per read, never a throw on timeout); MCP `join_room` / `send_room_message` / `read_room_messages` (carries a `wait` param) / `ack_room_message` / `claim_room_resource` / `release_room_claim`; CLI `run402 rooms join` + `run402 messages send|list|wait|get|ack` (`wait` is the blocking read) + `run402 claims create|list|release` — org-scoped coordination rooms, gateway `add-agent-messaging`) | `sdk/src/namespaces/rooms.ts` + `rooms.types.ts` (`waitForMessages`/`WaitForRoomMessagesOptions`/`RoomMessageWaitResult`, `ListRoomMessagesOptions.wait`, `RoomMessagePage.waited_ms`/`live_presences`, `program`/`model` on `RegisterPresenceOptions`/`SendRoomMessageInput`), `sdk/src/index.ts` (`Rooms` wiring + re-exports; the `scoped`/`forProject` room-bound `ScopedRoom` handles live on the namespace itself, including `waitForMessages`), `src/tools/{join-room,send-room-message,read-room-messages,ack-room-message,claim-room-resource,release-room-claim}.ts` + `src/tools/rooms-shared.ts` (room addressing + presence cache + harness labels on the retry-registration path) + `src/harness-context.ts` (`resolveHarnessLabels`) + `src/index.ts` (MCP registration), `cli/lib/rooms.mjs` + `cli/lib/messages.mjs` (`wait` subcommand) + `cli/lib/claims.mjs` + `cli/lib/rooms-context.mjs` (room resolution + `./.run402/messaging.json` presence/cursor cache + harness labels on registration) + `cli/lib/harness-context.mjs` (`resolveHarnessLabels`) + `cli/cli.mjs` (dispatch + top-level HELP) + `cli/lib/command-manifest.mjs` (`rooms`/`claims` families), `openclaw/scripts/rooms.mjs` + `openclaw/scripts/messages.mjs` + `openclaw/scripts/claims.mjs` (re-exports), `sync.test.ts` (`SURFACE` rows including `messages_wait` + `SDK_BY_CAPABILITY` + `rooms.listPresences`/`listClaims`/`getPresence`/`getMessage`/`scoped`/`forProject` in `SDK_ONLY_METHODS` + `rooms`/`claims` in the `parseCliCommands`/`parseOpenClawCommands` module lists), `sdk/src/namespaces/rooms-wait.test.ts` (settles/times-out/degrades against a fake gateway), `cli-messages-wait.test.mjs` (a holding fake gateway and one that answers at once), `cli-harness-context.test.mjs` + `src/harness-context.test.ts` (`resolveHarnessLabels`), docs-site cli/sdk/mcp reference sections (then `node scripts/build-agent-flat-docs.mjs` for [cli/llms-cli.txt](../../cli/llms-cli.txt) / [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) / [llms-mcp.txt](../../llms-mcp.txt)), [sdk/README.md](../../sdk/README.md) (catalog row), [README.md](../../README.md) (Agent messaging MCP tool table), [SKILL.md](../../SKILL.md) (coordination-rooms workflow section), [openclaw/SKILL.md](../../openclaw/SKILL.md). The CLI splits this into TWO families — `rooms` for the room itself (`join`) and `messages` for the messages in it (`send|list|wait|get|ack`). `rooms send`, `rooms ack`, `rooms list` and `rooms get` are RESERVED (`COMMAND_REMOVED` naming the successor), never aliased. `rooms list`/`get` are deliberately NOT reissued with room semantics — a freed spelling that silently answers with different data is worse than one that fails; message cursors are opaque `mcr_…` (store-and-echo; stale → `reset: true` + `earliest_cursor`); claims are ADVISORY (creation always succeeds with `conflicts[]` — never a block). Gateway routes (`/orgs/v1/:org_id/rooms/:room_key/*`) live in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Add or change the repos surface** (`gitvault`/`repos` is ONE noun, seventeen verbs, CLI + OpenClaw plus three read-only MCP tools (`handoff`/`resume`/`invite`/`join` are CLI/SDK-only, same "mutating verbs are CLI-only" law as `create`/`delete`); `invite`/`join` are a SECOND claim kind sharing the same crypto/keystore/restore/writer-admission machinery by `ClaimKind`, with their own `kgi1_` key prefix, `KGI1` envelope frame, room presence and the two product-posted facts; writers, plural (rev 47): `resume` activates the recipient's OWN key as a writer, `repos view` lists `writers[]`/`pending_writers[]`, `repos access sync` admits pending member keys, and `org member add` refuses a developer+ add it cannot admit; `r.gitvault` is the SDK's own name. `r.gitvault` reads `get`/`forProject`/`forRepo`/`resolveAddress`/`heads`/`allHeads`/`setPolicy`/`completeOverride`/`acquireMaintenanceLease`/`listByOrg` (the bulk vaults-by-org read, FROZEN shape) + Node-only `init`/`openOrCreate`/`resolveOrCreateAddress`/`push`/`status`/`compact`/`prune`/`verify`/`deploy`/`restore`/`scaffoldRemote`/`open`/`drainOverrides`/`fsck` (verify + ref materialization + `--mirror`, explicit `local_state_changed`/`pin_before`/`pin_after`, `write:false` for `--no-write`)/`access` (read-only recipients/coverage/local-TOFU-pin report; `envelope_state`/`history_scope` are not server-exposed) + the pure `gitvaultLossWarningTrip`/`gitvaultLossWarningTripped`/`gitvaultLossWarningMessage` helpers + the pure `gitvaultRemoteAddressForm`/`gitvaultRemoteUrlForRepo`/`gitvaultSlugReleasedInfo` helpers; MCP `repos_view` / `repos_list_heads` / `repos_fsck` (same handlers, one noun across every agent surface); CLI `run402 repos create|list|view|rename|delete|snapshot|policy|mirror|fsck|gc|access|recover|handoff|resume|invite|join` (`repo` singular resolves identically) + `run402 init [--git-remote]` + `run402 org slug` (the org-owned slug claim, CLI/SDK-only) and the `git-remote-run402` helper, which lazily allocates on first push and, for a slug-form `run402::<org-slug>/<name>` remote, resolves/push-to-creates through it while pinning `repo_id` in local git state on first resolution. `run402 gitvault <verb>` answers a structural `COMMAND_MOVED` (naming its `repos` successor) or, for `push`/`reconcile` (no behavioral successor), `COMMAND_REMOVED` — see `cli/lib/gitvault.mjs`. The host-blind encrypted Git remote, wire tag `r402s/v0`, gateway `add-gitvault`) | `sdk/src/namespaces/gitvault.ts` (`fsck`/`access`/`listByOrg`, `persist`/`write` options on `verify`) + `gitvault.crypto.ts` + `gitvault.types.ts`, `sdk/src/node/gitvault-publication.ts` (`verifyToNewest`/`materialize` `persist` option — `fsck --no-write`'s genuine audit mode; the BYO payload write path) + `sdk/src/node/gitvault-*.ts` (keystore, creation journal, snapshot, deploy, `gitvault-open-or-create.ts`, `gitvault-push-to-create.ts`, `gitvault-address.ts` [id-pinning + resolve-or-create orchestrator], `gitvault-byo-probe.ts` [allocation-time create-only destination probe, fail-closed] + `gitvault-byo-config.ts` [local BYO credential config, never transmitted] + `gitvault-mirror.ts`'s `chainOnlyEntries`/`byoChainCopySync`/`byoChainCopyPushForGeneration`/`verifyByoObjectsPresent` [chain-copy dual-write + the fsck absence check] + `gitvault-degraded-read.ts` [network-class fallback to mirror or BYO destination] — the three CLIENT-raised BYO codes `GITVAULT_BYO_BUCKET_PROBE_FAILED`/`GITVAULT_BYO_BUCKET_WRITE_REFUSED`/`GITVAULT_BYO_OBJECT_MISSING` live here, never in the closed protocol registry) + `sdk/src/node/gitvault-handoff.ts` (kind-parameterized by `ClaimKind` — the Handoff/Invite Key prefix registry + one `parseClaimKey`, the three kind-separated HKDF derivations (`auth`/`wrap`/`writer-admission`), the v2 sealed envelope seal/open per kind, the writer-admission grant + two-signature acceptance shared verbatim across kinds (the frozen `handoff` protocol spelling carries the invite id), and each kind's Note schema guard + secret-scan denylist) + `sdk/src/node/gitvault-restore.ts` (the stash-shaped checkpoint restore — `cloneGitvaultRemote` deliberately bypasses `hardenedGit`'s `protocol.allow=never` since the clone target IS the vault remote, `applyHandoffCheckpoint`'s `clone + git stash apply --index`, `excludeMessagingCacheFromGit`'s idempotent `.run402/` append to `.git/info/exclude` — used by both `resume` and `join`) + `sdk/src/node/gitvault-snapshot.ts`'s `captureHandoffSnapshot` (the stash-shaped 3-parent capture, the sensitive-path denylist), `cli/lib/gitvault-capabilities.mjs` (the marketing-claim capability ledger), `sdk/src/namespaces/org.ts` (`ScopedOrg.setSlug`) + `org.types.ts` (`SetOrgSlugResult`, `OrgSummary.slug`), `sdk/src/namespaces/projects.ts` (`Projects.setRepoName`, reached via `repos rename`) + `projects.types.ts` (`SetRepoNameResult`) + `sdk/src/scoped.ts` (`ScopedRun402.setRepoName`), `sdk/src/errors.ts` (`NextActionType` — `push_repo`/`verify_refs`/`submit_gc`/`use_moved_command`/`access_repair_pending`/`configure_mirror`/`join_invite`/`revoke_invite`/`wait_room`/`send_room_message`), `sdk/src/index.ts` (`Gitvault` wiring + re-exports), `src/tools/gitvault.ts` (MCP handlers) + `src/index.ts` (MCP registration) + `src/tools/gitvault-copy.test.ts` (**the copy gate — see below**), `cli/lib/repos.mjs` (the family's CLI home — all 17 verbs, including `handoff`/`resume`/`invite`/`join`) + `cli/lib/cold-start.mjs` (the x402/Base-Sepolia-only allowance→faucet→tier chain `repos create` folds in on `NO_ACTIVE_TIER`, the cold-start-with-zero-prior-state path) + `cli/lib/path-lookup.mjs` (`isExecutableOnPath`, used by `doctor`/`view`'s `git-remote-kygit`-on-PATH check) + `cli/lib/gitvault.mjs` (the tombstone dispatcher — `printKeystoreLocation` is its one export) + `cli/lib/gitvault-target.mjs` (the shared pin > remote > env > active-project targeting resolver `repos.mjs`'s `vaultTarget` and `doctor.mjs`'s gitvault check both compose) + `cli/lib/gitvault-scaffold.mjs` (the provision/up/repos remote-scaffold fold-in — `nested` / `nestedCommand` carry `up --nested` and `repos create --nested`, and a skip inside another repository forwards the SDK's `create_nested_repo` next action respelled to the caller's verb — and the shared `-b main` / pre-2.28 fallback `up.mjs`'s own separate `gitInitIfNeeded` mirrors; `Gitvault.scaffoldRemote({ nested })` / `init({ nested })` own the nested-repo + one-line `.git/info/exclude` behavior and the `nested` / `enclosing_toplevel` / `excluded_in_enclosing` result fields; `cli-up-repo-compose.test.mjs` + `cli-repos-surface.test.mjs` + `sdk/src/namespaces/gitvault-scaffold-remote.test.ts` cover it) + `cli/lib/next-actions.mjs` (`setOrgSlugAction`/`setRepoNameAction`) + `openclaw/scripts/repos.mjs` + `openclaw/scripts/gitvault.mjs` + `cli/lib/org.mjs` (`slug` subcommand) + `openclaw/scripts/org.mjs` + `cli/git-remote-run402.mjs` (address-form dispatch + the shared wallet-selection resolution) + `cli/lib/wallet-context.mjs` (`resolveWalletCore`/`enforceWalletExistsCore`, shared by the CLI and the remote helper) + `cli/cli.mjs` (dispatch — `repos`/`repo`/`gitvault` case blocks + top-level HELP) + `cli/lib/init.mjs` (the `--git-remote` scaffold) + `cli/lib/org-context.mjs` (`resolveOwningOrgId`/`resolveOrgId`, shared by all) + `cli/lib/doctor.mjs` (the `gitvault` DIAGNOSTIC CHECK NAME — unrelated to the CLI noun's own naming — including the progressive warning and repo-standing-in targeting via `gitvault-target.mjs`) + `cli/lib/command-manifest.mjs` (`repos:*` entries, `RESERVED_SUBCOMMANDS`'s `gitvault:*` entries, `SKIPPED_FAMILIES`'s `gitvault`/`repo` entries) + `core/src/keystore.ts` (active-project scope derives the current wallet's principal, matching `NodeCredentialsProvider`), `sync.test.ts` (`SURFACE` `repos_*` rows + `SDK_BY_CAPABILITY` + the `SDK_ONLY_METHODS` entries + the module-name arrays both `parseCliCommands`/`parseOpenClawCommands` loop over), `cli-repos-surface.test.mjs` (the family's e2e coverage, incl. the cold-start fold + handoff/resume/invite/join) + `cli-gitvault-retired.test.mjs` (the tombstone dispatcher + the naming-law conventions gate — `handoff`/`resume`/`invite`/`join` classify as no-analog verbs) + `scripts/build-gitvault-surface.test.mjs` (the capability-ledger key list) + `cli/lib/path-lookup.test.mjs` + `sdk/src/node/gitvault-handoff.test.ts` + `sdk/src/node/gitvault-restore.test.ts` + `sdk/src/namespaces/gitvault-resume-errors.test.ts` + `sdk/src/namespaces/gitvault-join-errors.test.ts` (gateway error-code propagation + the raw-key-never-leaks contract, per kind), docs-site cli/sdk reference sections (then `node scripts/build-agent-flat-docs.mjs` for [cli/llms-cli.txt](../../cli/llms-cli.txt) / [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt)), [sdk/README.md](../../sdk/README.md) (catalog row + the isomorphic/Node-split pattern section), [README.md](../../README.md) (pattern section + the read-only MCP tool table), [cli/README.md](../../cli/README.md) (`repos` row), [SKILL.md](../../SKILL.md) + [openclaw/SKILL.md](../../openclaw/SKILL.md) (then `node scripts/build-agent-skills-index.mjs`), [AGENTS.md](../../AGENTS.md) (the `### Gitvault` architecture section), [CHANGELOG.md](../../CHANGELOG.md). **CLAIMS ARE NORMATIVE, NOT DESCRIPTIVE.** The approved vocabulary is exactly three sentences — *"Run402 cannot decrypt your gitvault or repository history. Deployment artifacts remain a disclosed plaintext custody boundary."*, *"Activation requires vault admission by default; an explicit, audited override can bypass it."*, and retention as an *operational promise of the platform, not a cryptographic guarantee against it* — plus the scoped confidentiality sentence, the keystore-qualified durability sentence (*"The vault protects source history from host-side loss while a principal keystore survives."*), and the verbatim terminal-loss statement — plus gitvault-mirror-default's recommended-default vocabulary (the three-copies framing sentence, the `vault_unmirrored` finding statement's opening, and the `repos create` mirror setup hint, all canonical in `sdk/src/namespaces/gitvault.crypto.ts` and pinned by the copy gate), plus gitvault-byo-primary-bucket's BYO vocabulary (the rung-3 headline — legal ONLY where the BYO profile is being described, never as a blanket claim about managed vaults — the no-payload-copy disclosure, and the BYO `vault_unmirrored` remedy, same canonical file, same gate). Copy may shorten them only by omitting a whole clause, NEVER by paraphrase: each banned phrase is banned because it OVERCLAIMS, so a rewrite that reads better is a regression. `src/tools/gitvault-copy.test.ts` pins both directions across every doc surface — add new surfaces to its corpus. The banned CLASS matters more than the literal list: any copy that pitches surviving a lost laptop or machine must carry the keystore qualifier in the same file, because the natural English form of that overclaim contains no banned phrase and a literal scan cannot catch it. **Mutating verbs are CLI-only by design** (immutable generations with no undo, the one-shot recovery receipt, the once-returned lease `holder_token`, destructive prune, owner+step-up policy) — do not add an MCP tool for them. Gateway routes (`/gitvault/v1/*`, plus `/gitvault/v1/vaults/:vault_id/handoffs` mint/list/revoke + `/gitvault/v1/handoffs/:handoff_id/claim`) and the protocol/threat-model **working drafts** live in `run402-private`; the published byte-identical mirrors are [`docs/gitvault/`](../../docs/gitvault/README.md) here (re-copy on every protocol revision, same as the `test-vectors/r402s-v0/` set); mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/gitvault/`, `apps/kygit/` (kygit.com — the standalone KyGit brand site, same page-truth gate), `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`. **`kygit/` (the `@kychee/kygit` package — a separate npm workspace, own release cadence, no lockstep with the SDK/CLI/MCP triad)**: `kygit/kygit.mjs` (the brand-door shim — sets `RUN402_REMOTE_SCHEME=kygit` on every exec'd `run402 repos …` call) + `kygit/git-remote-kygit.mjs` (the SECOND bin this package ships — a ~30-line forwarder to the installed `run402` package's own `git-remote-run402.mjs`, argv unchanged, `RUN402_REMOTE_SCHEME=kygit` added to the environment; exists because `npm i -g @kychee/kygit` alone can never link a bin literally named `git-remote-run402`, so `kygit::` remotes need their own differently-named helper) + `kygit/kygit.test.mjs` + `kygit/git-remote-kygit.test.mjs` + `kygit/README.md` (one-package install line, the `kygit::`/`run402::` dual-spelling table, the `handoff`/`resume` mapping rows).
**Add or change `run402 up` / `r.up()` local behavior** (client detection + principal naming: `KNOWN_CLIENT_MARKERS` for `claude-code` / `codex` / `cursor` / `grok`, `RUN402_CLIENT=<name>` checked first, `RUN402_AGENT_NAME` / `identityName` overriding, `result.identity.detected` + `detection: { applied, reason }`; local validation: `--check` / every mode verifying referenced files → `MANIFEST_FILE_MISSING` + `create_file` next actions, `UP_MANIFEST_REQUIRED` with `nearby_manifests[]` + `run_in_directory`, `--manifest` to a missing path → `MANIFEST_NOT_FOUND`; repo composition incl. `--nested`) | `sdk/src/node/client-detect.ts` (the one marker table) + `sdk/src/node/actions-node.ts` + `sdk/src/actions.ts` (`Run402UpIdentity`, the `identity.name.set` step) + `sdk/src/node/deploy-manifest.ts` (`collectLocalFileReferences` / `findMissingLocalFileReferences` / `assertLocalFileReferencesExist` / `manifestFileMissingError` / `manifestNotFoundError`) + `sdk/src/node/index.ts` (exports) + `sdk/src/errors.ts` (`NextActionType`), `cli/lib/up.mjs` + `cli/lib/deploy-v2.mjs` (the same file check in `run402 deploy`) + `cli/lib/init.mjs` (`--name` help) + `cli/lib/gitvault-scaffold.mjs` (`--nested`), `src/tools/app-up.ts` (`display_name` / `no_rehearse` descriptions), `cli/lib/harness-context.mjs` + `src/harness-context.ts` (`resolveHarnessLabels` infers presence `program` from the same markers — keep the lists in step), tests `sdk/src/node/actions-node.test.ts` / `sdk/src/node/deploy-manifest.test.ts` / `cli-typed-config-modes.test.mjs` / `cli-up-repo-compose.test.mjs` / `cli-harness-context.test.mjs` / `src/harness-context.test.ts`, docs-site `cli/reference.md` (index: the `up` contract) + `cli/deploy.md` + `sdk/reference.md` + `mcp/reference.md` then `node scripts/build-agent-flat-docs.mjs`, [README.md](../../README.md), [cli/README.md](../../cli/README.md), [sdk/README.md](../../sdk/README.md), [openclaw/README.md](../../openclaw/README.md), [SKILL.md](../../SKILL.md), [openclaw/SKILL.md](../../openclaw/SKILL.md), [AGENTS.md](../../AGENTS.md) (`node/*.ts` + CLI bullets), [CHANGELOG.md](../../CHANGELOG.md).
**Add or change the forkable-apps / marketplace surface** (`r.apps` `browse`/`getApp`/`fork`/`publish`/`listVersions`/`updateVersion`/`deleteVersion`; MCP `browse_apps`/`get_app`/`fork_app`/`publish_app`/`list_versions`/`update_version`/`delete_version`; CLI `run402 apps browse|inspect|fork|publish|versions|update|delete` — vision pillars 3-4: the "one paid call, one independent fully-deployed copy" fork) | `sdk/src/namespaces/apps.ts`, `sdk/src/index.ts` (`r.apps` wiring + re-exports), `src/tools/{browse-apps,get-app,fork-app,publish-app,list-versions,update-version,delete-version}.ts` + `src/index.ts` (registration), `cli/lib/apps.mjs` + `cli/cli.mjs` (dispatch), `openclaw/scripts/apps.mjs` (re-export), `sync.test.ts` (`SURFACE` rows + `SDK_BY_CAPABILITY` entries), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) (`r.apps` section), [sdk/README.md](../../sdk/README.md) (catalog row + count), [llms-mcp.txt](../../llms-mcp.txt) (Apps tools), [SKILL.md](../../SKILL.md) (Apps marketplace section), [openclaw/SKILL.md](../../openclaw/SKILL.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [README.md](../../README.md) (Apps marketplace table) + [AGENTS.md](../../AGENTS.md) (namespace count). Gateway `/apps/v1` + `/fork/v1` + `/projects/v1/admin/:id/versions*` routes live in `run402-private`; mirror user-visible changes in `apps/marketing/llms-full.txt`, `apps/marketing/openapi.json`, `apps/marketing/updates.txt`, `apps/marketing/humans/changelog.html`.
**Change `@run402/functions` surface** | edit `packages/functions/src/` AND `packages/functions/README.md` in `kychee-com/run402-core`, publish `@run402/functions` from `run402-core`, then bump the Cloud gateway dependency in `kychee-com/run402-private`. In this public repo update [AGENTS.md](../../AGENTS.md) (Functions library section), [SKILL.md](../../SKILL.md), [llms-mcp.txt](../../llms-mcp.txt), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), and [openclaw/SKILL.md](../../openclaw/SKILL.md) (in-function helpers section in each).
**Change `@run402/astro` surface** (new `<Image>` / `<Run402Picture>` / `<Run402Image>` prop or `imageDefaults` option, a hosted-auth component prop on `<SignIn>` / `<SignUp>` / `<UserButton>` / `<AccountSecurity>` / `<SignedIn>` / `<SignedOut>`, an integration option, a new `R402_ASTRO_*` / `R402_AUTH_*` error code, data-driven pattern guidance, or a runtime helper) | edit `astro/src/` and [astro/README.md](../../astro/README.md) and [astro/CHANGELOG.md](../../astro/CHANGELOG.md). If the change touches the AssetRef-persistence vs manifest pattern guidance, also reflect it in [AGENTS.md](../../AGENTS.md) (Astro integration section). Publish via `/publish-astro` (OIDC Trusted Publisher → `.github/workflows/publish-astro.yml`) — independent of the SDK/CLI/MCP lockstep.
**Add SSR Runtime / cache layer surface** (run402 `cache` / `dev` / `doctor` / `init astro` / `logs --request-id`; `r.cache` SDK; SSR adapter hooks; R402_* error codes; cache hot-path debug headers) **or change the `logs` / `doctor` contracts** (`run402 logs --request-id <id>` with no function name = the SDK's project-wide `functions.logsByRequestId` fan-out, `req_`/`fnrun_`/`fnatt_` ids, `--since`, `--app` (default) / `--platform` / `--all` → the `origin` filter with `origin` on every entry via the exported `classifyFunctionLogLine` and `hidden` / `hint`; `functions logs [<name>]` optional name with `--request-id`; MCP `get_function_logs` optional `name` + `origin`; `run402 doctor` = `{ ok, blocking[], warnings[], checks[] }`, `severity` per check, `ok` ⇔ `blocking[]` empty, the fixed tier status vocabulary, advisory `TIER_MISSING_ON_OWN_ORG`) | edit `cli/lib/<group>.mjs` (impl — `cli/lib/logs.mjs`, `cli/lib/doctor.mjs`, `cli/lib/functions.mjs` for the shared origin flag + `resolveLogOriginFlag` / `platformHiddenHint`), `cli/cli.mjs` (dispatch), `openclaw/scripts/<group>.mjs` (CLI parity re-export), `sdk/src/namespaces/functions.ts` + `functions.types.ts` (`logs` `origin`, `logsByRequestId`, `classifyFunctionLogLine`) + `sdk/src/scoped.ts` + `sdk/src/index.ts` (re-export), `src/tools/get-function-logs.ts`, `sync.test.ts` (`logs: "functions.logsByRequestId"` in `SDK_BY_CAPABILITY`), tests `cli-doctor.test.mjs` / `cli-argv.test.mjs` / `sdk/src/namespaces/functions.test.ts` / `src/tools/get-function-logs.test.ts`, the docs-site cli slices (`ops.md` for `cache`/`dev`/`doctor`/`logs`, `errors.md` for the R402_* codes table, `functions.md` for `functions logs`) then regenerate [cli/llms-cli.txt](../../cli/llms-cli.txt) + slices + full, docs-site `sdk/**` + `mcp/reference.md` then regenerate, `sdk/src/namespaces/cache.ts` + `sdk/src/index.ts` (type re-export) + [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt) (`r.cache` namespace), `astro/src/` + [astro/README.md](../../astro/README.md) + [astro/CHANGELOG.md](../../astro/CHANGELOG.md) (preset + adapter + detectors + `<Run402Picture>` + `<Run402Image>` + hosted-auth components `<SignIn>` / `<SignUp>` / `<UserButton>` / `<AccountSecurity>` + `<SignedIn>` / `<SignedOut>` gates), [AGENTS.md](../../AGENTS.md) (namespace count + v1.52 architecture section), [README.md](../../README.md) (namespace count + SSR pitch), [SKILL.md](../../SKILL.md) + [openclaw/SKILL.md](../../openclaw/SKILL.md) (Astro SSR runtime + ISR cache pattern section), [llms.txt](../../llms.txt) (vision bullet), [sdk/README.md](../../sdk/README.md) (namespace catalog table — bump count + cache row), `sync.test.ts` (`SURFACE` capabilities + `SDK_BY_CAPABILITY` entries + module list in `parseCliCommands`/`parseOpenClawCommands` + `SDK_ONLY_METHODS` for flag-variant convenience methods). Cross-repo to `run402-private`: edit `packages/gateway/src/` (substrate + hot path + apply-v1 plumbing), `packages/functions/src/` (SDK ALS + `cache.*`), `openspec/changes/astro-ssr-runtime/`, `apps/marketing/llms-full.txt` (new `/cache/v1/*` endpoints), `apps/marketing/openapi.json` (cache endpoint schemas), `apps/marketing/updates.txt` + `apps/marketing/humans/changelog.html` (user-visible feature entry).
**Rename / remove a tool, command, flag, or method** | All of the above + verify [SKILL.test.ts](../../SKILL.test.ts) banned-pattern list catches it; cross-repo: drop a private `apps/marketing/updates.txt` entry if user-visible
**Change a pattern** (auth-as-SDLC, paste-and-go AssetRef, deploy-dir plan/commit, db(req) vs adminDb()) | [README.md](../../README.md) patterns section, both `SKILL.md` files, [cli/llms-cli.txt](../../cli/llms-cli.txt), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [llms-mcp.txt](../../llms-mcp.txt), [llms.txt](../../llms.txt) wayfinder if the pattern is part of the pitch
**Add a new integration surface** | [llms.txt](../../llms.txt) (the wayfinder is the source of truth for "what surfaces exist"), [README.md](../../README.md) integrations table, [AGENTS.md](../../AGENTS.md), a matching `llms-<surface>.txt` reference file
**Change pricing or limits** | [README.md](../../README.md), [SKILL.md](../../SKILL.md), [cli/llms-cli.txt](../../cli/llms-cli.txt), [sdk/llms-sdk.txt](../../sdk/llms-sdk.txt), [llms-mcp.txt](../../llms-mcp.txt), [llms.txt](../../llms.txt), [openclaw/SKILL.md](../../openclaw/SKILL.md). Cross-repo: private `apps/marketing/humans/*.html` (terms, pricing pages)
**Change architecture (kernel, error hierarchy, build flow)** | [AGENTS.md](../../AGENTS.md) is the canonical place; reflect any user-visible consequences in the relevant skill / reference
**Cross-repo (private repo)** — **change an HTTP endpoint, ship a user-visible feature, or change the API surface** | `apps/marketing/llms-full.txt` (HTTP API reference), `apps/marketing/openapi.json` if applicable, `apps/marketing/updates.txt` + `apps/marketing/humans/changelog.html` for user-visible features. Open a separate PR in `run402-private/`.

