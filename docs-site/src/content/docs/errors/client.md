---
title: client errors
description: Owned codes and recovery guidance.
---

[Error contracts and compatibility index](/errors/). Follow the actual response's message, field details and next actions. Never infer retry safety from the code name alone.

<h2 id="AMBIGUOUS_ORG">AMBIGUOUS_ORG</h2>

Owner: `public:cli/lib/org-context.mjs`, `public:cli/lib/orgs.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="APP_SPEC_INVALID">APP_SPEC_INVALID</h2>

Owner: `public:sdk/src/app-up.ts`, `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="APPROVE_BAD_CONFIRM_URL">APPROVE_BAD_CONFIRM_URL</h2>

Owner: `public:cli/lib/sign-in.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="APPROVE_FAILED">APPROVE_FAILED</h2>

Owner: `public:cli/lib/sign-in.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_DESCRIPTOR_MISSING">ARCHIVE_DESCRIPTOR_MISSING</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_DIGEST_MISMATCH">ARCHIVE_DIGEST_MISMATCH</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_DUPLICATE_PATH">ARCHIVE_DUPLICATE_PATH</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_ENTRY_TYPE_UNSUPPORTED">ARCHIVE_ENTRY_TYPE_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_MALFORMED_JSON">ARCHIVE_MALFORMED_JSON</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_MALFORMED_TAR">ARCHIVE_MALFORMED_TAR</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_MEDIA_TYPE_UNSUPPORTED">ARCHIVE_MEDIA_TYPE_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_PATH_UNSAFE">ARCHIVE_PATH_UNSAFE</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_SIZE_MISMATCH">ARCHIVE_SIZE_MISMATCH</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_UNSUPPORTED_REQUIRED_CAPABILITY">ARCHIVE_UNSUPPORTED_REQUIRED_CAPABILITY</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_UNSUPPORTED_VERSION">ARCHIVE_UNSUPPORTED_VERSION</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ARCHIVE_WAIT_TIMEOUT">ARCHIVE_WAIT_TIMEOUT</h2>

Owner: `public:sdk/src/namespaces/archives.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ASSET_DUPLICATE_KEY_IN_PUT">ASSET_DUPLICATE_KEY_IN_PUT</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ASSET_KEY_IN_PUT_AND_DELETE">ASSET_KEY_IN_PUT_AND_DELETE</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ASTRO_ADAPTER_TREE_IN_SITE">ASTRO_ADAPTER_TREE_IN_SITE</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_ENV">BAD_ENV</h2>

Owner: `public:cli/lib/credentials.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_FIELD">BAD_FIELD</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_FILE">BAD_FILE</h2>

Owner: `public:cli/lib/buzz.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_FLAG">BAD_FLAG</h2>

Owner: `public:cli/lib/approve.mjs`, `public:cli/lib/archives.mjs`, `public:cli/lib/argparse.mjs`, `public:cli/lib/assets.mjs`, `public:cli/lib/auth.mjs`, `public:cli/lib/billing.mjs`, `public:cli/lib/buzz-notifications.mjs`, `public:cli/lib/buzz.mjs`, `public:cli/lib/cdn.mjs`, `public:cli/lib/contracts.mjs`, `public:cli/lib/doctor.mjs`, `public:cli/lib/domains.mjs`, `public:cli/lib/email.mjs`, `public:cli/lib/errors.mjs`, `public:cli/lib/feedback.mjs`, `public:cli/lib/identity.mjs`, `public:cli/lib/pay.mjs`, `public:cli/lib/projects.mjs`, `public:cli/lib/repos.mjs`, `public:cli/lib/sites.mjs`, `public:cli/lib/transfer.mjs`, `public:cli/lib/up.mjs`, `public:cli/lib/wallet-context.mjs`, `public:cli/lib/wallets.mjs`, `public:cli/lib/webhooks.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_JSON">BAD_JSON</h2>

Owner: `public:cli/lib/auth.mjs`, `public:cli/lib/jobs.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_JSON_FLAG">BAD_JSON_FLAG</h2>

Owner: `public:cli/lib/sdk-errors.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_ORG_ID">BAD_ORG_ID</h2>

Owner: `public:cli/lib/org-context.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_PRIVATE_KEY">BAD_PRIVATE_KEY</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_PROJECT_ID">BAD_PROJECT_ID</h2>

Owner: `public:cli/lib/argparse.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_PROJECT_NAME">BAD_PROJECT_NAME</h2>

Owner: `public:cli/lib/projects.mjs`, `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_REQUEST">BAD_REQUEST</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_USAGE">BAD_USAGE</h2>

Owner: `public:sdk/src/namespaces/credentials.ts`, `public:cli/lib/admin.mjs`, `public:cli/lib/agent.mjs`, `public:cli/lib/ai.mjs`, `public:cli/lib/approve.mjs`, `public:cli/lib/apps.mjs`, `public:cli/lib/archives.mjs`, `public:cli/lib/argparse.mjs`, `public:cli/lib/assets.mjs`, `public:cli/lib/auth.mjs`, `public:cli/lib/billing.mjs`, `public:cli/lib/branches.mjs`, `public:cli/lib/buzz.mjs`, `public:cli/lib/cache.mjs`, `public:cli/lib/cdn.mjs`, `public:cli/lib/ci.mjs`, `public:cli/lib/contacts.mjs`, `public:cli/lib/contracts.mjs`, `public:cli/lib/credentials.mjs`, `public:cli/lib/deliveries.mjs`, `public:cli/lib/deploy-v2.mjs`, `public:cli/lib/dev.mjs`, `public:cli/lib/doctor.mjs`, `public:cli/lib/domains.mjs`, `public:cli/lib/email.mjs`, `public:cli/lib/errors.mjs`, `public:cli/lib/events.mjs`, `public:cli/lib/feedback.mjs`, `public:cli/lib/functions.mjs`, `public:cli/lib/image.mjs`, `public:cli/lib/init-astro.mjs`, `public:cli/lib/init.mjs`, `public:cli/lib/jobs.mjs`, `public:cli/lib/live.mjs`, `public:cli/lib/login.mjs`, `public:cli/lib/logout.mjs`, `public:cli/lib/logs.mjs`, `public:cli/lib/org-context.mjs`, `public:cli/lib/orgs.mjs`, `public:cli/lib/pay.mjs`, `public:cli/lib/projects.mjs`, `public:cli/lib/redeem.mjs`, `public:cli/lib/repos.mjs`, `public:cli/lib/rooms-context.mjs`, `public:cli/lib/rooms.mjs`, `public:cli/lib/secrets.mjs`, `public:cli/lib/sites.mjs`, `public:cli/lib/snapshots.mjs`, `public:cli/lib/status.mjs`, `public:cli/lib/subdomains.mjs`, `public:cli/lib/subscriptions.mjs`, `public:cli/lib/tier.mjs`, `public:cli/lib/transfer.mjs`, `public:cli/lib/up.mjs`, `public:cli/lib/wallets.mjs`, `public:cli/lib/webhooks.mjs`, `public:cli/lib/whoami.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_WALLET_FILE">BAD_WALLET_FILE</h2>

Owner: `public:cli/lib/config.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_WALLET_NAME">BAD_WALLET_NAME</h2>

Owner: `public:cli/lib/wallet-context.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BAD_WEBHOOK_URL">BAD_WEBHOOK_URL</h2>

Owner: `public:cli/lib/argparse.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BINARY_CONTENT_REQUIRES_BYTES">BINARY_CONTENT_REQUIRES_BYTES</h2>

Owner: `public:sdk/src/namespaces/assets.ts`, `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BUILD_APPROVAL_REQUIRED">BUILD_APPROVAL_REQUIRED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="BUZZ_ADOPTION_OFFERS_UNSUPPORTED">BUZZ_ADOPTION_OFFERS_UNSUPPORTED</h2>

Owner: `public:cli/lib/buzz.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CANNOT_REBUILD_UNLOCKED_DEPS">CANNOT_REBUILD_UNLOCKED_DEPS</h2>

Owner: `public:cli/lib/functions.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CI_PROJECT_REQUIRED">CI_PROJECT_REQUIRED</h2>

Owner: `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CI_SUBJECT_REQUIRED">CI_SUBJECT_REQUIRED</h2>

Owner: `public:cli/lib/ci.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="COMMAND_MOVED">COMMAND_MOVED</h2>

Owner: `public:cli/lib/gitvault.mjs`, `public:cli/lib/projects.mjs`, `public:cli/lib/source-access.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="COMMAND_REMOVED">COMMAND_REMOVED</h2>

Owner: `public:cli/lib/escalations.mjs`, `public:cli/lib/gitvault.mjs`, `public:cli/lib/notifications.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CONFIG_ENV_REQUIRED">CONFIG_ENV_REQUIRED</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CONFIRMATION_REQUIRED">CONFIRMATION_REQUIRED</h2>

Owner: `public:cli/lib/contracts.mjs`, `public:cli/lib/domains.mjs`, `public:cli/lib/email.mjs`, `public:cli/lib/projects.mjs`, `public:cli/lib/repos.mjs`, `public:cli/lib/subdomains.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="CONTENT_UPLOAD_FAILED">CONTENT_UPLOAD_FAILED</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="DELEGATE_SCOPE_INSUFFICIENT">DELEGATE_SCOPE_INSUFFICIENT</h2>

Owner: `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="DOMAIN_WAIT_TIMEOUT">DOMAIN_WAIT_TIMEOUT</h2>

Owner: `public:sdk/src/namespaces/domains.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="DRY_RUN_PLAN_NOT_COMMITTABLE">DRY_RUN_PLAN_NOT_COMMITTABLE</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EMPTY_BODY">EMPTY_BODY</h2>

Owner: `public:cli/lib/jobs.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EXECUTABLE_CONFIG_EVALUATION_FAILED">EXECUTABLE_CONFIG_EVALUATION_FAILED</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EXECUTABLE_CONFIG_INVALID_EXPORT">EXECUTABLE_CONFIG_INVALID_EXPORT</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EXECUTABLE_CONFIG_LOAD_FAILED">EXECUTABLE_CONFIG_LOAD_FAILED</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EXECUTABLE_CONFIG_TIMEOUT">EXECUTABLE_CONFIG_TIMEOUT</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="EXECUTABLE_CONFIG_TS_LOADER_UNAVAILABLE">EXECUTABLE_CONFIG_TS_LOADER_UNAVAILABLE</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="FAUCET_FAILED">FAUCET_FAILED</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="FILE_NOT_FOUND">FILE_NOT_FOUND</h2>

Owner: `public:cli/lib/argparse.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="FUNCTION_BODY_INVALID_JSON">FUNCTION_BODY_INVALID_JSON</h2>

Owner: `public:cli/lib/functions.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="FUNCTION_BODY_SOURCE_CONFLICT">FUNCTION_BODY_SOURCE_CONFLICT</h2>

Owner: `public:cli/lib/functions.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GIT_COMMAND_FAILED">GIT_COMMAND_FAILED</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GIT_CWD_MISSING">GIT_CWD_MISSING</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GIT_UNAVAILABLE">GIT_UNAVAILABLE</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITHUB_REPO_REQUIRED">GITHUB_REPO_REQUIRED</h2>

Owner: `public:cli/lib/ci.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITHUB_REPOSITORY_ID_REQUIRED">GITHUB_REPOSITORY_ID_REQUIRED</h2>

Owner: `public:cli/lib/ci.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_ACTIVATION_TOKEN_MISSING">GITVAULT_ACTIVATION_TOKEN_MISSING</h2>

Owner: `public:sdk/src/node/gitvault-apply.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_BAD_GENESIS">GITVAULT_BAD_GENESIS</h2>

Owner: `public:sdk/src/node/gitvault-writer-state.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_BAD_ID">GITVAULT_BAD_ID</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_BYO_OBJECT_MISSING">GITVAULT_BYO_OBJECT_MISSING</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_CAPTURE_RECEIPT_MISSING">GITVAULT_CAPTURE_RECEIPT_MISSING</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_COMPACT_INSUFFICIENT_HEADROOM">GITVAULT_COMPACT_INSUFFICIENT_HEADROOM</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_COMPACTION_IN_PROGRESS">GITVAULT_COMPACTION_IN_PROGRESS</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_CONFIG_INCLUDE_REFUSED">GITVAULT_CONFIG_INCLUDE_REFUSED</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_CONFIG_UNREADABLE">GITVAULT_CONFIG_UNREADABLE</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_DEPLOY_ABANDONED">GITVAULT_DEPLOY_ABANDONED</h2>

Owner: `public:sdk/src/node/gitvault-apply.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_EPOCH_NOT_OPENABLE">GITVAULT_EPOCH_NOT_OPENABLE</h2>

Owner: `public:sdk/src/node/gitvault-publication.ts`, `public:sdk/src/node/gitvault-recover.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_EXCLUDES_PATH_UNSUPPORTED">GITVAULT_EXCLUDES_PATH_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_EXCLUDES_UNREADABLE">GITVAULT_EXCLUDES_UNREADABLE</h2>

Owner: `public:sdk/src/node/gitvault-snapshot.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_IDENTITY_PROFILE_MISMATCH">GITVAULT_IDENTITY_PROFILE_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_KEY_ROTATION_REQUIRED">GITVAULT_KEY_ROTATION_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_LANE_OUT_OF_ORDER">GITVAULT_LANE_OUT_OF_ORDER</h2>

Owner: `public:sdk/src/node/gitvault-apply.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_MIRROR_AMBIGUOUS">GITVAULT_MIRROR_AMBIGUOUS</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_MIRROR_EMPTY">GITVAULT_MIRROR_EMPTY</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_MIRROR_NOT_CONFIGURED">GITVAULT_MIRROR_NOT_CONFIGURED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_NODE_ONLY">GITVAULT_NODE_ONLY</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_OBJECT_READ_FAILED">GITVAULT_OBJECT_READ_FAILED</h2>

Owner: `public:sdk/src/node/gitvault-publication.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_ORG_UNRESOLVED">GITVAULT_ORG_UNRESOLVED</h2>

Owner: `public:cli/lib/repos.mjs`, `public:cli/lib/up.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_OVERRIDE_COMPLETION_MISMATCH">GITVAULT_OVERRIDE_COMPLETION_MISMATCH</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_OVERRIDE_COMPLETION_REFUSED">GITVAULT_OVERRIDE_COMPLETION_REFUSED</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_PIN_MANIFEST_READ_UNSUPPORTED">GITVAULT_PIN_MANIFEST_READ_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/gitvault-publication.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_PLAN_DIGEST_MISSING">GITVAULT_PLAN_DIGEST_MISSING</h2>

Owner: `public:sdk/src/node/gitvault-apply.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_POLICY_UNSET">GITVAULT_POLICY_UNSET</h2>

Owner: `public:sdk/src/node/gitvault-apply.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_PROOF_OF_OPEN_PRINCIPAL_UNRESOLVED">GITVAULT_PROOF_OF_OPEN_PRINCIPAL_UNRESOLVED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_RECEIPT_ID_REUSED_DIFFERENT">GITVAULT_RECEIPT_ID_REUSED_DIFFERENT</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_REPO_STATE_MISSING">GITVAULT_REPO_STATE_MISSING</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_SIGNATURE_INVALID">GITVAULT_SIGNATURE_INVALID</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_TOKEN_BINDING_MISMATCH">GITVAULT_TOKEN_BINDING_MISMATCH</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_UNSUPPORTED_TARGET">GITVAULT_UNSUPPORTED_TARGET</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="GITVAULT_VAULT_UNRESOLVED">GITVAULT_VAULT_UNRESOLVED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="HANDOFF_CLONE_FAILED">HANDOFF_CLONE_FAILED</h2>

Owner: `public:sdk/src/node/gitvault-restore.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="HANDOFF_ENVELOPE_INVALID">HANDOFF_ENVELOPE_INVALID</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="HANDOFF_ID_MISMATCH">HANDOFF_ID_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="HANDOFF_KEY_REVOKED">HANDOFF_KEY_REVOKED</h2>

Owner: `public:sdk/src/node/gitvault-writer-state.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="HANDOFF_MINT_GRANT_MISMATCH">HANDOFF_MINT_GRANT_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IDENTITY_LINK_EOA_SIGNER_MISMATCH">IDENTITY_LINK_EOA_SIGNER_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/identity-links.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IDENTITY_LINK_EOA_SIGNER_REQUIRED">IDENTITY_LINK_EOA_SIGNER_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/identity-links.ts`, `public:sdk/src/node/credentials.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IDENTITY_LINK_EVENT_SOURCE_REQUIRED">IDENTITY_LINK_EVENT_SOURCE_REQUIRED</h2>

Owner: `public:cli/lib/identity.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IDENTITY_LINK_INVALID_UTF8">IDENTITY_LINK_INVALID_UTF8</h2>

Owner: `public:cli/lib/identity.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IDENTITY_LINK_PUBLIC_VISIBILITY_REQUIRED">IDENTITY_LINK_PUBLIC_VISIBILITY_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/identity-links.ts`, `public:cli/lib/identity.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IMPORT_CONFORMANCE_FAILED">IMPORT_CONFORMANCE_FAILED</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="IMPORT_VERIFY_FAILED">IMPORT_VERIFY_FAILED</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVALID_ASSET_METADATA">INVALID_ASSET_METADATA</h2>

Owner: `public:sdk/src/namespaces/assets-validation.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVALID_ENV_FILE">INVALID_ENV_FILE</h2>

Owner: `public:sdk/src/node/archives-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVALID_EXIF_POLICY">INVALID_EXIF_POLICY</h2>

Owner: `public:sdk/src/namespaces/assets-validation.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVALID_PAYMENT_ATTEMPT_ID">INVALID_PAYMENT_ATTEMPT_ID</h2>

Owner: `public:sdk/src/node/payment-attempts.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVALID_VERIFY_SPEC">INVALID_VERIFY_SPEC</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVITE_ENVELOPE_INVALID">INVITE_ENVELOPE_INVALID</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVITE_ID_MISMATCH">INVITE_ID_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVITE_MINT_GRANT_MISMATCH">INVITE_MINT_GRANT_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="INVITE_REDEEM_WRITER_KEY_MISMATCH">INVITE_REDEEM_WRITER_KEY_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LIGHTNING_WALLET_STILL_MINTING">LIGHTNING_WALLET_STILL_MINTING</h2>

Owner: `public:sdk/src/namespaces/agent.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOCAL_CREDENTIAL_CACHE_UNSUPPORTED">LOCAL_CREDENTIAL_CACHE_UNSUPPORTED</h2>

Owner: `public:sdk/src/namespaces/credentials.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOCAL_ROUTE_TARGET_UNDECLARED">LOCAL_ROUTE_TARGET_UNDECLARED</h2>

Owner: `public:sdk/src/node/preflight.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOGIN_DENIED">LOGIN_DENIED</h2>

Owner: `public:cli/lib/login.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOGIN_EXPIRED">LOGIN_EXPIRED</h2>

Owner: `public:cli/lib/login.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOGIN_FAILED">LOGIN_FAILED</h2>

Owner: `public:cli/lib/login.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOGIN_REQUIRED">LOGIN_REQUIRED</h2>

Owner: `public:cli/lib/sign-in.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOGIN_TIMEOUT">LOGIN_TIMEOUT</h2>

Owner: `public:cli/lib/login.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="LOOPBACK_FAILED">LOOPBACK_FAILED</h2>

Owner: `public:cli/lib/login.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MAILBOX_NOT_FOUND">MAILBOX_NOT_FOUND</h2>

Owner: `public:cli/lib/email.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MANIFEST_EMPTY">MANIFEST_EMPTY</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MANIFEST_EXPORT_UNSUPPORTED">MANIFEST_EXPORT_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/manifest-export.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MANIFEST_FILE_MISSING">MANIFEST_FILE_MISSING</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MANIFEST_NOT_FOUND">MANIFEST_NOT_FOUND</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`, `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MANIFEST_OUT_WRITE_FAILED">MANIFEST_OUT_WRITE_FAILED</h2>

Owner: `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MERCHANT_RECEIPT_REQUIRED">MERCHANT_RECEIPT_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/pay.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MERCHANT_RECEIPT_UNAVAILABLE">MERCHANT_RECEIPT_UNAVAILABLE</h2>

Owner: `public:sdk/src/namespaces/pay.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MESSAGE_TOO_LONG">MESSAGE_TOO_LONG</h2>

Owner: `public:cli/lib/feedback.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="MISSING_SECRET">MISSING_SECRET</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NETWORK_ERROR">NETWORK_ERROR</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NO_ORGS">NO_ORGS</h2>

Owner: `public:cli/lib/orgs.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NO_PRESENCE">NO_PRESENCE</h2>

Owner: `public:cli/lib/rooms.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NO_PROJECT">NO_PROJECT</h2>

Owner: `public:cli/lib/transfer.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NO_WALLET">NO_WALLET</h2>

Owner: `public:cli/lib/ci.mjs`, `public:cli/lib/config.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NOT_A_FILE">NOT_A_FILE</h2>

Owner: `public:cli/lib/argparse.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="NOT_FOUND">NOT_FOUND</h2>

Owner: `public:cli/lib/contacts.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ORG_REQUIRED">ORG_REQUIRED</h2>

Owner: `public:cli/lib/org-context.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ORG_UNRESOLVED">ORG_UNRESOLVED</h2>

Owner: `public:cli/lib/orgs.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="OTHER">OTHER</h2>

Owner: `public:sdk/src/node/nwc.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_ATTEMPT_READ_FAILED">PAYMENT_ATTEMPT_READ_FAILED</h2>

Owner: `public:sdk/src/node/payment-attempts.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_ATTEMPT_RECORD_INVALID">PAYMENT_ATTEMPT_RECORD_INVALID</h2>

Owner: `public:sdk/src/node/payment-attempts.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_DEPENDENCY_MISSING">PAYMENT_DEPENDENCY_MISSING</h2>

Owner: `public:sdk/src/namespaces/pay.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_EXCEEDS_MAX">PAYMENT_EXCEEDS_MAX</h2>

Owner: `public:sdk/src/namespaces/pay.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_NETWORK_UNSUPPORTED">PAYMENT_NETWORK_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_SETTLEMENT_FAILED">PAYMENT_SETTLEMENT_FAILED</h2>

Owner: `public:sdk/src/namespaces/pay.ts`, `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_SOURCE_CONFLICT">PAYMENT_SOURCE_CONFLICT</h2>

Owner: `public:sdk/src/node/index.ts`, `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PAYMENT_WALLET_UNFUNDED">PAYMENT_WALLET_UNFUNDED</h2>

Owner: `public:sdk/src/namespaces/pay.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PROJECT_CREDENTIAL_EXPIRED">PROJECT_CREDENTIAL_EXPIRED</h2>

Owner: `public:sdk/src/errors.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PROJECT_CREDENTIAL_INVALID">PROJECT_CREDENTIAL_INVALID</h2>

Owner: `public:sdk/src/errors.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PROJECT_CREDENTIAL_NOT_FOUND">PROJECT_CREDENTIAL_NOT_FOUND</h2>

Owner: `public:sdk/src/errors.ts`, `public:cli/lib/config.mjs`, `public:cli/lib/credentials.mjs`, `public:cli/lib/sdk-errors.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PROJECT_HAS_NON_REPO_RESOURCES">PROJECT_HAS_NON_REPO_RESOURCES</h2>

Owner: `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PROJECT_REQUIRED">PROJECT_REQUIRED</h2>

Owner: `public:sdk/src/node/actions-node.ts`, `public:cli/lib/config.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="PRUNE_CONFIRMATION_REQUIRED">PRUNE_CONFIRMATION_REQUIRED</h2>

Owner: `public:sdk/src/node/assets-node.ts`, `public:cli/lib/sites.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RAIL_SWITCH_REQUIRES_CONFIRM">RAIL_SWITCH_REQUIRES_CONFIRM</h2>

Owner: `public:cli/lib/init.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REHEARSAL_FAILED">REHEARSAL_FAILED</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REMOTE_BUILD_UNSUPPORTED">REMOTE_BUILD_UNSUPPORTED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REMOVED_COMMAND">REMOVED_COMMAND</h2>

Owner: `public:cli/lib/projects.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REPOS_NAME_AMBIGUOUS">REPOS_NAME_AMBIGUOUS</h2>

Owner: `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REPOS_NAME_REQUIRED">REPOS_NAME_REQUIRED</h2>

Owner: `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="REVEAL_REQUIRED">REVEAL_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/credentials.ts`, `public:cli/lib/credentials.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ROOM_INVITE_ID_MISMATCH">ROOM_INVITE_ID_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/rooms.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ROOM_INVITE_KEY_CONFERS_SEAT">ROOM_INVITE_KEY_CONFERS_SEAT</h2>

Owner: `public:sdk/src/namespaces/rooms.types.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ROOM_REQUIRED">ROOM_REQUIRED</h2>

Owner: `public:cli/lib/rooms-context.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ROOMS_NODE_ONLY">ROOMS_NODE_ONLY</h2>

Owner: `public:sdk/src/namespaces/rooms.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="ROTATION_COUNTERS_REQUIRED">ROTATION_COUNTERS_REQUIRED</h2>

Owner: `public:cli/lib/repos.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_ACTION_FAILED">RUN402_ACTION_FAILED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_ACTION_UNKNOWN">RUN402_ACTION_UNKNOWN</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_PROJECT_CONFLICT">RUN402_PROJECT_CONFLICT</h2>

Owner: `public:sdk/src/node/actions-node.ts`, `public:cli/lib/up.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_PROJECT_KEYS_REQUIRED">RUN402_PROJECT_KEYS_REQUIRED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_TARGET_ANON_KEY_REQUIRED">RUN402_TARGET_ANON_KEY_REQUIRED</h2>

Owner: `public:sdk/src/node/target-profile.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_TARGET_MISMATCH">RUN402_TARGET_MISMATCH</h2>

Owner: `public:sdk/src/node/target-profile.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_TARGET_PROJECT_REQUIRED">RUN402_TARGET_PROJECT_REQUIRED</h2>

Owner: `public:sdk/src/node/target-profile.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_TARGET_SERVICE_KEY_REQUIRED">RUN402_TARGET_SERVICE_KEY_REQUIRED</h2>

Owner: `public:sdk/src/node/target-profile.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_WORKSPACE_LINK_CONFLICT">RUN402_WORKSPACE_LINK_CONFLICT</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_WORKSPACE_LINK_INVALID">RUN402_WORKSPACE_LINK_INVALID</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="RUN402_WORKSPACE_LINK_SYMLINK">RUN402_WORKSPACE_LINK_SYMLINK</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="SESSION_INVALID">SESSION_INVALID</h2>

Owner: `public:cli/lib/whoami.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="SPEND_APPROVAL_REQUIRED">SPEND_APPROVAL_REQUIRED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="SUBDOMAIN_MULTI_NOT_SUPPORTED">SUBDOMAIN_MULTI_NOT_SUPPORTED</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="TYPESCRIPT_FUNCTION_REQUIRES_BUNDLE">TYPESCRIPT_FUNCTION_REQUIRES_BUNDLE</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNKNOWN">UNKNOWN</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `public:sdk/src/node/gitvault-deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNKNOWN_FLAG">UNKNOWN_FLAG</h2>

Owner: `public:cli/lib/argparse.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNKNOWN_SUBCOMMAND">UNKNOWN_SUBCOMMAND</h2>

Owner: `public:cli/lib/argparse.mjs`, `public:cli/lib/gitvault.mjs`, `public:cli/lib/source-access.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSAFE_SECRET_MANIFEST">UNSAFE_SECRET_MANIFEST</h2>

Owner: `public:cli/lib/deploy-v2.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSUPPORTED_CI_FLAG">UNSUPPORTED_CI_FLAG</h2>

Owner: `public:cli/lib/ci.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSUPPORTED_DEPLOY_MANIFEST_EXTENSION">UNSUPPORTED_DEPLOY_MANIFEST_EXTENSION</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSUPPORTED_EXECUTABLE_CONFIG_EXTENSION">UNSUPPORTED_EXECUTABLE_CONFIG_EXTENSION</h2>

Owner: `public:sdk/src/node/deploy-manifest.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSUPPORTED_OPTION">UNSUPPORTED_OPTION</h2>

Owner: `public:cli/lib/sites.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UNSUPPORTED_RESOURCE_KIND">UNSUPPORTED_RESOURCE_KIND</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="UP_PROJECT_REQUIRED">UP_PROJECT_REQUIRED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="VALIDATION_ERROR">VALIDATION_ERROR</h2>

Owner: `public:sdk/src/namespaces/transfers.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="VERDICT_UNAVAILABLE">VERDICT_UNAVAILABLE</h2>

Owner: `public:cli/lib/errors.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="VERIFY_FAILED">VERIFY_FAILED</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="VERIFY_PROPAGATION_PENDING">VERIFY_PROPAGATION_PENDING</h2>

Owner: `public:sdk/src/node/actions-node.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_EXISTS">WALLET_EXISTS</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_LABEL_DRIFT">WALLET_LABEL_DRIFT</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_NOT_FOUND">WALLET_NOT_FOUND</h2>

Owner: `public:cli/lib/wallet-context.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_PROTECTED">WALLET_PROTECTED</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_RENAME_FAILED">WALLET_RENAME_FAILED</h2>

Owner: `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WALLET_SELECTION_CONFLICT">WALLET_SELECTION_CONFLICT</h2>

Owner: `public:cli/lib/wallet-context.mjs`, `public:cli/lib/wallets.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WILDCARD_ROUTE_EXCLUDES_MUTATION_METHODS">WILDCARD_ROUTE_EXCLUDES_MUTATION_METHODS</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="WORKFLOW_EXISTS">WORKFLOW_EXISTS</h2>

Owner: `public:cli/lib/ci.mjs`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="X402_AMOUNT_EXCEEDS_FIRST_PARTY_CAP">X402_AMOUNT_EXCEEDS_FIRST_PARTY_CAP</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="X402_ATTEMPT_ID_ALREADY_EXISTS">X402_ATTEMPT_ID_ALREADY_EXISTS</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="X402_ATTEMPT_ID_COLLISION">X402_ATTEMPT_ID_COLLISION</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="X402_ATTEMPT_JOURNAL_FAILED">X402_ATTEMPT_JOURNAL_FAILED</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.

<h2 id="X402_PAYMENT_OUTCOME_AMBIGUOUS">X402_PAYMENT_OUTCOME_AMBIGUOUS</h2>

Owner: `public:sdk/src/node/paid-fetch.ts`.

The client could not complete this operation. Inspect its typed error kind, message, input fields and next actions. Local preflight, authentication, transport and ambiguous mutation outcomes need different repairs; do not infer that no remote operation occurred merely because the error was rendered by a client.
