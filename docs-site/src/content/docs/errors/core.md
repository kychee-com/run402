---
title: core errors
description: Owned codes and recovery guidance.
---

[Error contracts and compatibility index](/errors/). Follow the actual response's message, field details and next actions. Never infer retry safety from the code name alone.

<h2 id="astro_ssr_unsupported_feature">astro_ssr_unsupported_feature</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="dependency_install_failed">dependency_install_failed</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="dependency_install_rejected">dependency_install_rejected</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="dynamic_runtime_busy">dynamic_runtime_busy</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="dynamic_runtime_timeout">dynamic_runtime_timeout</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="dynamic_runtime_unavailable">dynamic_runtime_unavailable</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="local_executor_failed">local_executor_failed</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="missing_required_secret">missing_required_secret</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="project_not_found">project_not_found</h2>

Owner: `core:packages/runtime-kernel/src/projects.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="request_body_too_large">request_body_too_large</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="response_body_too_large">response_body_too_large</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.

<h2 id="unsupported_capability">unsupported_capability</h2>

Owner: `core:packages/runtime-kernel/src/errors.ts`.

The selected Core runtime cannot complete this operation in its current configuration. Inspect its capability report and error details; configure the named dependency or use a supported operation. Cloud-only capabilities are not implied by the shared SDK.
