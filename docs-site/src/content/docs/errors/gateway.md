---
title: gateway errors
description: Owned codes and recovery guidance.
---

[Error contracts and compatibility index](/errors/). Follow the actual response's message, field details and next actions. Never infer retry safety from the code name alone.

<h2 id="ABOVE_WRITE_AUTH_CEILING">ABOVE_WRITE_AUTH_CEILING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ACCEPT_PAYMENT_INVALID">ACCEPT_PAYMENT_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ACCEPT_PAYMENT_TOO_LARGE">ACCEPT_PAYMENT_TOO_LARGE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ACCEPT_PAYMENT_TOO_MANY_FIELDS">ACCEPT_PAYMENT_TOO_MANY_FIELDS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ACTIVATION_EXPIRED">ACTIVATION_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ADMIN_REQUIRED">ADMIN_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ADMIN_SQL_CAPABILITY_GATE_FAILED">ADMIN_SQL_CAPABILITY_GATE_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ADMIN_SQL_CHANNEL_UNCONFIGURED">ADMIN_SQL_CHANNEL_UNCONFIGURED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ADMIN_SQL_READ_ONLY">ADMIN_SQL_READ_ONLY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ALLOCATION_SUPERSEDED">ALLOCATION_SUPERSEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AMBIGUOUS_EXTERNAL_SELECTION">AMBIGUOUS_EXTERNAL_SELECTION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AMBIGUOUS_INBOUND_DOMAIN">AMBIGUOUS_INBOUND_DOMAIN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AMBIGUOUS_MAILBOX">AMBIGUOUS_MAILBOX</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AMBIGUOUS_OPERATOR_RECIPIENT">AMBIGUOUS_OPERATOR_RECIPIENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="APP_INSTALL_STATE_NOT_FOUND">APP_INSTALL_STATE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ARCHIVE_BLOB_MISSING">ARCHIVE_BLOB_MISSING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ARCHIVE_CAS_UNAVAILABLE">ARCHIVE_CAS_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ARCHIVE_EXPIRED">ARCHIVE_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ASSET_SYNC_DRIFT">ASSET_SYNC_DRIFT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ATTEMPT_DESCRIPTOR_CONFLICT">ATTEMPT_DESCRIPTOR_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AUTH_CREDENTIALS_AMBIGUOUS">AUTH_CREDENTIALS_AMBIGUOUS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AUTH_REQUIRED">AUTH_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AUTHORIZATION_EPOCH_STALE">AUTHORIZATION_EPOCH_STALE</h2>

Owner: `public:sdk/src/node/gitvault-deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="AUTHORIZATION_UNSUPPORTED">AUTHORIZATION_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BASE_RELEASE_NOT_FOUND">BASE_RELEASE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BROKER_SECRET_UNSET">BROKER_SECRET_UNSET</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BROKER_SIGNATURE_INVALID">BROKER_SIGNATURE_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_ATTEMPT_EXPIRED">BUZZ_ADOPTION_ATTEMPT_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_CALLBACK_INVALID">BUZZ_ADOPTION_CALLBACK_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_NOT_ELIGIBLE">BUZZ_ADOPTION_NOT_ELIGIBLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_OFFER_INELIGIBLE">BUZZ_ADOPTION_OFFER_INELIGIBLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_OFFER_NOT_ACTIVE">BUZZ_ADOPTION_OFFER_NOT_ACTIVE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ADOPTION_TARGET_MISMATCH">BUZZ_ADOPTION_TARGET_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_AGENT_PROFILE_MISMATCH">BUZZ_AGENT_PROFILE_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_COMMUNITY_PROOF_INVALID">BUZZ_COMMUNITY_PROOF_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_COMMUNITY_PROOF_UNSUPPORTED">BUZZ_COMMUNITY_PROOF_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ENROLLMENT_APPROVAL_REQUIRED">BUZZ_ENROLLMENT_APPROVAL_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ENROLLMENT_POLICY_DENIED">BUZZ_ENROLLMENT_POLICY_DENIED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_ENROLLMENT_SCOPE_UNSUPPORTED">BUZZ_ENROLLMENT_SCOPE_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_IDEMPOTENCY_CONFLICT">BUZZ_IDEMPOTENCY_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_IDENTITY_DRIFT">BUZZ_IDENTITY_DRIFT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_INSTALLATION_STALE">BUZZ_INSTALLATION_STALE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_INVALID_REQUEST">BUZZ_INVALID_REQUEST</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_OWNER_PROOF_INVALID">BUZZ_OWNER_PROOF_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_OWNER_PROOF_REQUIRED">BUZZ_OWNER_PROOF_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_RATE_LIMITED">BUZZ_RATE_LIMITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_RESOURCE_FORBIDDEN">BUZZ_RESOURCE_FORBIDDEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="BUZZ_SECRET_INPUT_FORBIDDEN">BUZZ_SECRET_INPUT_FORBIDDEN</h2>

Owner: `public:sdk/src/namespaces/buzz.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CHAIN_BROKEN">CHAIN_BROKEN</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CHAIN_UNUSABLE">CHAIN_UNUSABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CHECKPOINT_INCOMPLETE">CHECKPOINT_INCOMPLETE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CHECKPOINT_SET_LIMIT_EXCEEDED">CHECKPOINT_SET_LIMIT_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_CHALLENGE_INVALID">CLAIM_CHALLENGE_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_NOTHING_TO_CLAIM">CLAIM_NOTHING_TO_CLAIM</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_SELF">CLAIM_SELF</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_SOURCE_DISABLED">CLAIM_SOURCE_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_SOURCE_NOT_AGENT">CLAIM_SOURCE_NOT_AGENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLAIM_SOURCE_TOO_COMPLEX">CLAIM_SOURCE_TOO_COMPLEX</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLIENT_DIAGNOSTIC_EVENT_INVALID">CLIENT_DIAGNOSTIC_EVENT_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLIENT_DIAGNOSTIC_RATE_LIMITED">CLIENT_DIAGNOSTIC_RATE_LIMITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLIENT_OPEN_ID_CONFLICT">CLIENT_OPEN_ID_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CLIENT_VERSION_UNSUPPORTED">CLIENT_VERSION_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CONTENT_DIGEST_MISMATCH">CONTENT_DIGEST_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CONTENT_ENCODING_NOT_SUPPORTED">CONTENT_ENCODING_NOT_SUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CREDENTIAL_MISSING">CREDENTIAL_MISSING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CREDENTIAL_NAME_TAKEN">CREDENTIAL_NAME_TAKEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CREDENTIAL_NOT_FOUND">CREDENTIAL_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CREDENTIAL_REVOKED">CREDENTIAL_REVOKED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CUT_STORAGE_INTEGRITY_FAILURE">CUT_STORAGE_INTEGRITY_FAILURE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="CYCLE_REPLACEMENT_PENDING">CYCLE_REPLACEMENT_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DEFAULT_MAILBOX_INVALID">DEFAULT_MAILBOX_INVALID</h2>

Owner: `public:sdk/src/namespaces/email.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DEFAULT_MAILBOX_REQUIRED">DEFAULT_MAILBOX_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/email.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DEPLOY_BLOCKED_PUSH_FAILED">DEPLOY_BLOCKED_PUSH_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DEPLOY_INVALID_ROLE_GATE">DEPLOY_INVALID_ROLE_GATE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DESIRED_RECIPIENTS_NOT_RECONCILED">DESIRED_RECIPIENTS_NOT_RECONCILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DEVICE_CODE_NOT_FOUND">DEVICE_CODE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DIFF_SAME_RELEASE">DIFF_SAME_RELEASE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="DRY_RUN_REQUIRES_INLINE_SPEC">DRY_RUN_REQUIRES_INLINE_SPEC</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EDGE_ORIGIN_AUTH_REQUIRED">EDGE_ORIGIN_AUTH_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ELEVATION_REQUIRED">ELEVATION_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EMAIL_BELONGS_TO_OTHER_PRINCIPAL">EMAIL_BELONGS_TO_OTHER_PRINCIPAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EMAIL_SEND_IDENTITY_REGISTRATION_FAILED">EMAIL_SEND_IDENTITY_REGISTRATION_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ENROLLMENT_REQUIRES_SESSION">ENROLLMENT_REQUIRES_SESSION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EPOCH_ROTATION_INCOMPLETE_ENROLLMENT">EPOCH_ROTATION_INCOMPLETE_ENROLLMENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EPOCH_ROTATION_REQUIRED">EPOCH_ROTATION_REQUIRED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EPOCH_ROTATION_SELF_OPEN_UNPROVEN">EPOCH_ROTATION_SELF_OPEN_UNPROVEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EPOCH_ROTATION_WOULD_LEAVE_VAULT_UNCOVERED">EPOCH_ROTATION_WOULD_LEAVE_VAULT_UNCOVERED</h2>

Owner: `public:sdk/src/node/gitvault-writer-state.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EXPORT_CONSISTENCY_UNAVAILABLE">EXPORT_CONSISTENCY_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EXPORT_REDACTION_FAILED">EXPORT_REDACTION_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="EXPORT_SCOPE_UNSUPPORTED">EXPORT_SCOPE_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FLOW_NOT_FOUND">FLOW_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FLOW_NOT_PENDING">FLOW_NOT_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FORBIDDEN">FORBIDDEN</h2>

Owner: `public:sdk/src/namespaces/ci.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FREE_ORG_OWNER_LIMIT_EXCEEDED">FREE_ORG_OWNER_LIMIT_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FUNCTION_IN_ACTIVE_RELEASE">FUNCTION_IN_ACTIVE_RELEASE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="FUNCTION_RUN_ROUTE_NOT_FOUND">FUNCTION_RUN_ROUTE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GC_EPOCH_STALE">GC_EPOCH_STALE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GENERATION_REGRESSION">GENERATION_REGRESSION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GIT_OBJECT_FORMAT_UNSUPPORTED">GIT_OBJECT_FORMAT_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GIT_OBJECT_TOO_LARGE">GIT_OBJECT_TOO_LARGE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_ACCESS_DENIED">GITVAULT_ACCESS_DENIED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_BYO_PAYLOAD_NOT_HELD">GITVAULT_BYO_PAYLOAD_NOT_HELD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_BYO_PRUNE_NOT_SUPPORTED">GITVAULT_BYO_PRUNE_NOT_SUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_BYO_SINGLE_WRITER">GITVAULT_BYO_SINGLE_WRITER</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_CLIENT_UPGRADE_REQUIRED">GITVAULT_CLIENT_UPGRADE_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_ENVELOPE_RECIPIENT_NOT_ELIGIBLE">GITVAULT_ENVELOPE_RECIPIENT_NOT_ELIGIBLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_SIGNER_NOT_CONFIGURED">GITVAULT_SIGNER_NOT_CONFIGURED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_STORAGE_NOT_CONFIGURED">GITVAULT_STORAGE_NOT_CONFIGURED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GITVAULT_WRITER_NOT_ADMITTED">GITVAULT_WRITER_NOT_ADMITTED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `public:sdk/src/node/gitvault-writer-state.ts`, `public:cli/lib/org.mjs`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="GRANT_NOT_FOUND">GRANT_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="HANDOFF_CLAIM_WRITER_KEY_MISMATCH">HANDOFF_CLAIM_WRITER_KEY_MISMATCH</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="HANDOFF_MINT_REQUIRES_WRITER">HANDOFF_MINT_REQUIRES_WRITER</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="HEAD_CAS_CONFLICT">HEAD_CAS_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDEMPOTENCY_KEY_DUPLICATE">IDEMPOTENCY_KEY_DUPLICATE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDEMPOTENCY_KEY_INVALID">IDEMPOTENCY_KEY_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDEMPOTENCY_KEY_PAYER_REQUIRED">IDEMPOTENCY_KEY_PAYER_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDEMPOTENCY_KEY_REQUIRED">IDEMPOTENCY_KEY_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDEMPOTENCY_KEY_REUSED">IDEMPOTENCY_KEY_REUSED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_AGENT_REQUIRED">IDENTITY_LINK_AGENT_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_ATTEMPTS_EXHAUSTED">IDENTITY_LINK_ATTEMPTS_EXHAUSTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_AUTHENTICATOR_CHANGED">IDENTITY_LINK_AUTHENTICATOR_CHANGED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_AUTHENTICATOR_MISMATCH">IDENTITY_LINK_AUTHENTICATOR_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_CAPABILITY_UNAVAILABLE">IDENTITY_LINK_CAPABILITY_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_CHALLENGE_EXPIRED">IDENTITY_LINK_CHALLENGE_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_CHALLENGE_LIMIT">IDENTITY_LINK_CHALLENGE_LIMIT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_CHALLENGE_NOT_FOUND">IDENTITY_LINK_CHALLENGE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_CROSS_BINDING_FAILED">IDENTITY_LINK_CROSS_BINDING_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_DUPLICATE_FIELD">IDENTITY_LINK_DUPLICATE_FIELD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_EOA_REQUIRED">IDENTITY_LINK_EOA_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_EVENT_INCOMPLETE">IDENTITY_LINK_EVENT_INCOMPLETE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_EVENT_NOT_CANONICAL">IDENTITY_LINK_EVENT_NOT_CANONICAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_EVENT_TAGS_NOT_ALLOWED">IDENTITY_LINK_EVENT_TAGS_NOT_ALLOWED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_IDEMPOTENCY_CONFLICT">IDENTITY_LINK_IDEMPOTENCY_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_CURSOR">IDENTITY_LINK_INVALID_CURSOR</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_EVENT">IDENTITY_LINK_INVALID_EVENT</h2>

Owner: `public:sdk/src/namespaces/identity-links.protocol.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_JSON">IDENTITY_LINK_INVALID_JSON</h2>

Owner: `public:cli/lib/identity.mjs`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_NIP_OA">IDENTITY_LINK_INVALID_NIP_OA</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_PAYLOAD">IDENTITY_LINK_INVALID_PAYLOAD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_INVALID_WALLET_SIGNATURE">IDENTITY_LINK_INVALID_WALLET_SIGNATURE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_NONCANONICAL">IDENTITY_LINK_NONCANONICAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_NONCE_REPLAYED">IDENTITY_LINK_NONCE_REPLAYED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_NOT_FOUND">IDENTITY_LINK_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_PRINCIPAL_CONFLICT">IDENTITY_LINK_PRINCIPAL_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_PROOF_INVALID">IDENTITY_LINK_PROOF_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_RATE_LIMITED">IDENTITY_LINK_RATE_LIMITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_READS_DISABLED">IDENTITY_LINK_READS_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_REQUIRES_AGENT_EOA">IDENTITY_LINK_REQUIRES_AGENT_EOA</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_REQUIRES_HUMAN_SESSION">IDENTITY_LINK_REQUIRES_HUMAN_SESSION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_REVOKE_DISABLED">IDENTITY_LINK_REVOKE_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_SECRET_INPUT_FORBIDDEN">IDENTITY_LINK_SECRET_INPUT_FORBIDDEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_SUBJECT_CONFLICT">IDENTITY_LINK_SUBJECT_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_UNKNOWN_FIELD">IDENTITY_LINK_UNKNOWN_FIELD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IDENTITY_LINK_WRITES_DISABLED">IDENTITY_LINK_WRITES_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IMAGE_DECODE_FAILED">IMAGE_DECODE_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IMAGE_ENCODE_TIMEOUT">IMAGE_ENCODE_TIMEOUT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="IMAGE_INPUT_TOO_LARGE">IMAGE_INPUT_TOO_LARGE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INBOUND_DOMAIN_ALREADY_ENABLED">INBOUND_DOMAIN_ALREADY_ENABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INSUFFICIENT_BALANCE">INSUFFICIENT_BALANCE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INTERNAL_ERROR">INTERNAL_ERROR</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_ASSET_KEY">INVALID_ASSET_KEY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_AUTH">INVALID_AUTH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_CAPABILITY">INVALID_CAPABILITY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_CONTENT">INVALID_CONTENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_CURSOR">INVALID_CURSOR</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_CURSOR_FOR_SORT">INVALID_CURSOR_FOR_SORT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_DIFF_TARGET">INVALID_DIFF_TARGET</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_DISPLAY_NAME">INVALID_DISPLAY_NAME</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_DOMAIN_AUTHORITY">INVALID_DOMAIN_AUTHORITY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_EMAIL">INVALID_EMAIL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_FILTER_KEY">INVALID_FILTER_KEY</h2>

Owner: `public:sdk/src/namespaces/assets-validation.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_FILTER_VALUE">INVALID_FILTER_VALUE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_GRANT">INVALID_GRANT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_IDEMPOTENCY_KEY">INVALID_IDEMPOTENCY_KEY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_LABEL">INVALID_LABEL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_PKCE">INVALID_PKCE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_PLAN_MODE">INVALID_PLAN_MODE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_PRINCIPAL_ID">INVALID_PRINCIPAL_ID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_QUERY_PARAM">INVALID_QUERY_PARAM</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_RANGE_HEADER">INVALID_RANGE_HEADER</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_REDIRECT_URI">INVALID_REDIRECT_URI</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_REHEARSAL_OPTIONS">INVALID_REHEARSAL_OPTIONS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_REQUIRED_PLAN">INVALID_REQUIRED_PLAN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_RESOLVE_REQUEST">INVALID_RESOLVE_REQUEST</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_ROLE">INVALID_ROLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_SECRET_NAME">INVALID_SECRET_NAME</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_SNAPSHOT_OPTION">INVALID_SNAPSHOT_OPTION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_SORT">INVALID_SORT</h2>

Owner: `public:sdk/src/namespaces/assets-validation.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_SPEC">INVALID_SPEC</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVALID_STATE">INVALID_STATE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="INVITE_NOT_FOUND">INVITE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ISSUER_DAILY_MINT_CAP_REACHED">ISSUER_DAILY_MINT_CAP_REACHED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ISSUER_DISABLED">ISSUER_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ISSUER_OUTSTANDING_CAP_REACHED">ISSUER_OUTSTANDING_CAP_REACHED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="KEY_NOT_PENDING">KEY_NOT_PENDING</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="KEY_ROTATION_NOT_SELF_AUTHORIZABLE">KEY_ROTATION_NOT_SELF_AUTHORIZABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="KEY_ROTATION_REQUIRED">KEY_ROTATION_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="KEYSTORE_MISSING">KEYSTORE_MISSING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LAST_OWNER">LAST_OWNER</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LAST_WRAPPER">LAST_WRAPPER</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_BEARER_INELIGIBLE">LIGHTNING_BEARER_INELIGIBLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_TOPUP_AMOUNT_OUT_OF_BOUNDS">LIGHTNING_TOPUP_AMOUNT_OUT_OF_BOUNDS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_TOPUP_NOT_CONFIGURED">LIGHTNING_TOPUP_NOT_CONFIGURED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_TOPUP_NOT_FOUND">LIGHTNING_TOPUP_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_TOPUP_PROVIDER_UNAVAILABLE">LIGHTNING_TOPUP_PROVIDER_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_WALLET_NOT_AVAILABLE">LIGHTNING_WALLET_NOT_AVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIGHTNING_WALLET_NOT_FOUND">LIGHTNING_WALLET_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="LIVE_CONNECTION_LIMIT">LIVE_CONNECTION_LIMIT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MAINTENANCE_CYCLE_RESTART_REQUIRED">MAINTENANCE_CYCLE_RESTART_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MAINTENANCE_LEASE_HELD">MAINTENANCE_LEASE_HELD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MAINTENANCE_STAGE_LIMIT_EXCEEDED">MAINTENANCE_STAGE_LIMIT_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MAX_EPOCH_ROTATION_ENVELOPES_EXCEEDED">MAX_EPOCH_ROTATION_ENVELOPES_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MEMBER_NOT_FOUND">MEMBER_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MERGE_SELF">MERGE_SELF</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MERGE_SOURCE_SESSION_STALE">MERGE_SOURCE_SESSION_STALE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="METHOD_NOT_ALLOWED">METHOD_NOT_ALLOWED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MIGRATE_GATE_ACTIVE">MIGRATE_GATE_ACTIVE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MIGRATION_BREAKS_ROLE_GATE">MIGRATION_BREAKS_ROLE_GATE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MIGRATION_CHECKSUM_MISMATCH">MIGRATION_CHECKSUM_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MIGRATION_FAILED">MIGRATION_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="MPP_LIGHTNING_UNAVAILABLE">MPP_LIGHTNING_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NO_ACTIVE_ENCRYPTION_KEY">NO_ACTIVE_ENCRYPTION_KEY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NO_ACTIVE_RELEASE">NO_ACTIVE_RELEASE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOSTR_IDENTITY_ALREADY_LINKED">NOSTR_IDENTITY_ALREADY_LINKED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOT_A_HUMAN_PRINCIPAL">NOT_A_HUMAN_PRINCIPAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOT_A_RECIPIENT">NOT_A_RECIPIENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOT_A_RECIPIENT_PRINCIPAL">NOT_A_RECIPIENT_PRINCIPAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOT_IMPLEMENTED">NOT_IMPLEMENTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="NOT_RESUMABLE">NOT_RESUMABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPEN_PROOF_MISMATCH">OPEN_PROOF_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPERATION_CURSOR_NOT_FOUND">OPERATION_CURSOR_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPERATION_NOT_FOUND">OPERATION_NOT_FOUND</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPERATOR_EMAIL_NOT_VERIFIED">OPERATOR_EMAIL_NOT_VERIFIED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPERATOR_PASSKEY_REQUIRED">OPERATOR_PASSKEY_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OPERATOR_REAUTH_REQUIRED">OPERATOR_REAUTH_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORG_HAS_NO_BILLING_WALLET">ORG_HAS_NO_BILLING_WALLET</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORG_NOT_FOUND">ORG_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORGANIZATION_NOT_FOUND">ORGANIZATION_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORGANIZATION_PURGE_IN_PROGRESS">ORGANIZATION_PURGE_IN_PROGRESS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORGANIZATION_PURGE_INCOMPLETE">ORGANIZATION_PURGE_INCOMPLETE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ORGANIZATION_SELECTION_REQUIRED">ORGANIZATION_SELECTION_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="OVERRIDE_NOT_AUTHORIZED">OVERRIDE_NOT_AUTHORIZED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PASSKEY_ENROLLMENT_REQUIRED">PASSKEY_ENROLLMENT_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_AUTHORIZATION_LIFETIME_EXCEEDED">PAYMENT_AUTHORIZATION_LIFETIME_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_BODY_NOT_REPLAYABLE">PAYMENT_BODY_NOT_REPLAYABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_BODY_TOO_LARGE">PAYMENT_BODY_TOO_LARGE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_CALLER_IDENTITY_NOT_ACTIVE">PAYMENT_CALLER_IDENTITY_NOT_ACTIVE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_CHALLENGE_MISMATCH">PAYMENT_CHALLENGE_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_CREDENTIAL_EXPIRED">PAYMENT_CREDENTIAL_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_CREDENTIAL_INVALID">PAYMENT_CREDENTIAL_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_CREDITED">PAYMENT_CREDITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_DESTINATION_DRAINING">PAYMENT_DESTINATION_DRAINING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_EVIDENCE_UNAVAILABLE">PAYMENT_EVIDENCE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="payment_insufficient_funds">payment_insufficient_funds</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_INTENT_DESTINATION_CHANGED">PAYMENT_INTENT_DESTINATION_CHANGED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_INTENT_FENCE_EXPIRED">PAYMENT_INTENT_FENCE_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_INTENT_PENDING">PAYMENT_INTENT_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_INVOICE_PROVISIONING_RETRY">PAYMENT_INVOICE_PROVISIONING_RETRY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_ISSUANCE_UNAVAILABLE">PAYMENT_ISSUANCE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_MANUAL_REVIEW">PAYMENT_MANUAL_REVIEW</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_OPERATION_CONTRACT_CHANGED">PAYMENT_OPERATION_CONTRACT_CHANGED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_PREIMAGE_HASH_MISMATCH">PAYMENT_PREIMAGE_HASH_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_PROFILE_INVALID">PAYMENT_PROFILE_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_QUOTE_SOURCE_INVALID">PAYMENT_QUOTE_SOURCE_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_QUOTE_SOURCE_UNAVAILABLE">PAYMENT_QUOTE_SOURCE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_QUOTE_STALE">PAYMENT_QUOTE_STALE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_RECOVERY_PENDING">PAYMENT_RECOVERY_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_REQUIRED">PAYMENT_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_SEMANTIC_HEADER_DUPLICATE">PAYMENT_SEMANTIC_HEADER_DUPLICATE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_SEMANTIC_HEADER_INVALID">PAYMENT_SEMANTIC_HEADER_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PAYMENT_STATE_UNAVAILABLE">PAYMENT_STATE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PIN_CHANGE_UNCONFIRMED">PIN_CHANGE_UNCONFIRMED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PIN_MANIFEST_STORAGE_INTEGRITY_FAILURE">PIN_MANIFEST_STORAGE_INTEGRITY_FAILURE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PLAN_ALREADY_COMMITTED">PLAN_ALREADY_COMMITTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PLAN_EXPIRED">PLAN_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PLAN_NOT_FOUND">PLAN_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PLAN_REQUIRES_INLINE_SPEC">PLAN_REQUIRES_INLINE_SPEC</h2>

Owner: `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="POLICY_CHANGE_NOT_AUTHORIZED">POLICY_CHANGE_NOT_AUTHORIZED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="POSSESSION_PROOF_INVALID">POSSESSION_PROOF_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PRESENCE_EXPIRED">PRESENCE_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PRF_EGRESS_REJECTED">PRF_EGRESS_REJECTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PRINCIPAL_NOSTR_IDENTITY_ALREADY_LINKED">PRINCIPAL_NOSTR_IDENTITY_ALREADY_LINKED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PRINCIPAL_NOT_FOUND">PRINCIPAL_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PRIVATE_KEY_INPUT_FORBIDDEN">PRIVATE_KEY_INPUT_FORBIDDEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_CREDENTIAL_PROJECT_MISMATCH">PROJECT_CREDENTIAL_PROJECT_MISMATCH</h2>

Owner: `public:sdk/src/errors.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_DORMANT">PROJECT_DORMANT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_FROZEN">PROJECT_FROZEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_HAS_NO_ORGANIZATION">PROJECT_HAS_NO_ORGANIZATION</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_HAS_PENDING_TRANSFER">PROJECT_HAS_PENDING_TRANSFER</h2>

Owner: `public:sdk/src/errors.ts`, `public:sdk/src/namespaces/deploy.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_NOT_FOUND">PROJECT_NOT_FOUND</h2>

Owner: `public:cli/lib/sdk-errors.mjs`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_NOT_READY">PROJECT_NOT_READY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROJECT_PAST_DUE">PROJECT_PAST_DUE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROMO_LIMIT_REACHED">PROMO_LIMIT_REACHED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROMOTE_DEPLOYMENT_UNSERVABLE">PROMOTE_DEPLOYMENT_UNSERVABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROMOTE_PROJECT_MISMATCH">PROMOTE_PROJECT_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PROMOTE_TARGET_NOT_FOUND">PROMOTE_TARGET_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="PUBLIC_MINT_NOT_ENABLED">PUBLIC_MINT_NOT_ENABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="QUOTA_EXCEEDED">QUOTA_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="RATE_LIMITED">RATE_LIMITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="RECIPIENT_SET_MISMATCH">RECIPIENT_SET_MISMATCH</h2>

Owner: `public:sdk/src/node/gitvault-writer-state.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REF_EXPECTED_OLD_MISMATCH">REF_EXPECTED_OLD_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REF_STATE_LIMIT_EXCEEDED">REF_STATE_LIMIT_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REFNAME_UNSUPPORTED">REFNAME_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REGISTRY_ROLLBACK">REGISTRY_ROLLBACK</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REHEARSAL_CI_UNSUPPORTED">REHEARSAL_CI_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REHEARSAL_CONTENT_MISSING">REHEARSAL_CONTENT_MISSING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="RELINK_SOURCE_ORG_NOT_EMPTY">RELINK_SOURCE_ORG_NOT_EMPTY</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REPAIR_FENCE_PENDING">REPAIR_FENCE_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REPAIR_NOT_AUTHORIZED">REPAIR_NOT_AUTHORIZED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REPAIR_TARGET_UNPRESERVABLE">REPAIR_TARGET_UNPRESERVABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REPO_CREATION_CONFLICT">REPO_CREATION_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REPO_NAME_TAKEN">REPO_NAME_TAKEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="RESOURCE_NOT_FOUND">RESOURCE_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="REVIEWED_PLAN_ID_MISMATCH">REVIEWED_PLAN_ID_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROLE_FORBIDDEN">ROLE_FORBIDDEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_CLAIM_REQUIRES_WALLET">ROOM_INVITE_CLAIM_REQUIRES_WALLET</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_ID_CONFLICT">ROOM_INVITE_ID_CONFLICT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_KEY_ALREADY_CLAIMED">ROOM_INVITE_KEY_ALREADY_CLAIMED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_KEY_EXPIRED">ROOM_INVITE_KEY_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_KEY_INVALID">ROOM_INVITE_KEY_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_KEY_REVOKED">ROOM_INVITE_KEY_REVOKED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROOM_INVITE_OPEN_LIMIT">ROOM_INVITE_OPEN_LIMIT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROTATION_ID_MISMATCH">ROTATION_ID_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROUTED_FUNCTION_INVOKE_FAILED">ROUTED_FUNCTION_INVOKE_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="ROUTED_PUBLIC_RATE_LIMITED">ROUTED_PUBLIC_RATE_LIMITED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SERVICE_UNAVAILABLE">SERVICE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SESSION_EXPIRED">SESSION_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SLUG_PROTECTED">SLUG_PROTECTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SLUG_RELEASED">SLUG_RELEASED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SLUG_RESERVED">SLUG_RESERVED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SLUG_TAKEN">SLUG_TAKEN</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SNAPSHOT_CONFLICTED_INDEX">SNAPSHOT_CONFLICTED_INDEX</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="STAGE_CANDIDATE_RESERVED">STAGE_CANDIDATE_RESERVED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="STEP_UP_REQUIRED">STEP_UP_REQUIRED</h2>

Owner: `public:cli/lib/operator.mjs`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="STORAGE_UNAVAILABLE">STORAGE_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="SUITE_UNSUPPORTED">SUITE_UNSUPPORTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TABLE_NOT_EXPOSED">TABLE_NOT_EXPOSED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TABLE_NOT_LIVE">TABLE_NOT_LIVE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TEST_MODE_DISABLED">TEST_MODE_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TEST_USER_LIMIT">TEST_USER_LIMIT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TIER_PAYMENT_IDENTITY_REQUIRED">TIER_PAYMENT_IDENTITY_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TIER_PAYMENT_IDENTITY_REUSED">TIER_PAYMENT_IDENTITY_REUSED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TIER_PAYMENT_INTENT_MISMATCH">TIER_PAYMENT_INTENT_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TOO_MANY_ENCODES_QUEUED">TOO_MANY_ENCODES_QUEUED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TRANSFER_NOT_FOUND">TRANSFER_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TRANSITION_NOT_ACTIVE">TRANSITION_NOT_ACTIVE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="TREASURY_LOW">TREASURY_LOW</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="UNKNOWN_CREDENTIAL">UNKNOWN_CREDENTIAL</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="UNKNOWN_FIELD">UNKNOWN_FIELD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="UNKNOWN_RECIPIENT">UNKNOWN_RECIPIENT</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="UPGRADE_REQUIRED">UPGRADE_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="UPSTREAM_UNAVAILABLE">UPSTREAM_UNAVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VALIDATION_FAILED">VALIDATION_FAILED</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `public:sdk/src/node/gitvault-writer-state.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VAULT_CREATION_CONFLICT">VAULT_CREATION_CONFLICT</h2>

Owner: `public:sdk/src/node/gitvault-creation-journal.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VAULT_UNRECOVERABLE">VAULT_UNRECOVERABLE</h2>

Owner: `public:sdk/src/namespaces/gitvault.ts`, `public:sdk/src/node/gitvault-keystore.ts`, `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VERIFICATION_BUDGET_EXCEEDED">VERIFICATION_BUDGET_EXCEEDED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VOUCHER_ALREADY_REDEEMED">VOUCHER_ALREADY_REDEEMED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VOUCHER_AMOUNT_EXCEEDS_CAP">VOUCHER_AMOUNT_EXCEEDS_CAP</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VOUCHER_EXPIRED">VOUCHER_EXPIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="VOUCHER_NOT_FOUND">VOUCHER_NOT_FOUND</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WALLET_AUTH_REQUIRED">WALLET_AUTH_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WALLET_PROOF_INVALID">WALLET_PROOF_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WALLET_SIGNER_MISMATCH">WALLET_SIGNER_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRAPPER_EXISTS">WRAPPER_EXISTS</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRAPPER_NOT_PENDING">WRAPPER_NOT_PENDING</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_BINDING_MISMATCH">WRITE_AUTH_BINDING_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_CHALLENGE_INVALID">WRITE_AUTH_CHALLENGE_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_CLAIM_INVALID">WRITE_AUTH_CLAIM_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_CLASS_REJECTED">WRITE_AUTH_CLASS_REJECTED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_CP_SESSION_DEAD">WRITE_AUTH_CP_SESSION_DEAD</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_DISABLED">WRITE_AUTH_DISABLED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_NOT_AVAILABLE">WRITE_AUTH_NOT_AVAILABLE</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_REQUIRED">WRITE_AUTH_REQUIRED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_SCOPE_MISMATCH">WRITE_AUTH_SCOPE_MISMATCH</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_SESSION_INVALID">WRITE_AUTH_SESSION_INVALID</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.

<h2 id="WRITE_AUTH_VERIFY_FAILED">WRITE_AUTH_VERIFY_FAILED</h2>

Owner: `gateway:packages/gateway/src/utils/error-envelope.ts`.

The gateway rejected or could not complete the request. Its response carries the operation-specific explanation and repair fields. Follow those next actions using the intended principal and project. Inspect mutation outcome before retrying; payment challenges and permission denials require different repairs.
