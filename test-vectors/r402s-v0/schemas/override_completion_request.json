{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "$id": "override_completion_request.json",
 "title": "r402s/v0 override completion request API message (POST /gitvault/v1/vaults/:vault_id/override-completions); presents a later capture_receipt against the journaled unvaulted operation, which clears only when EVERY journaled field (repo, capture_id, apply_plan_sha256, snapshot_oid_hmac, head hash) equals the receipt's (DRAFT rev 41)",
 "type": "object",
 "additionalProperties": false,
 "required": [
  "operation_id",
  "capture_receipt"
 ],
 "properties": {
  "operation_id": {
   "type": "string",
   "minLength": 1
  },
  "capture_receipt": {
   "$ref": "capture_receipt.json"
  }
 },
 "x-r402s-revision": "41"
}
